# Java 场景面试题 · 单册

> 从《Java 面试全栈通关指南》中抽出的场景模块 ｜ 汇编日期：2026-09-13 ｜ 题目总数：**31**
> ｜ 难度分布：L1 入门 **4** · L2 进阶 **11** · L3 深入 **16**

> 每题七段式：难度 · 目标能力 · 标签 → 业务场景与量化约束 → 渐进式追问 → 参考答案要点 → 可接受的关键结论 → 考察意图 → 常见错误回答

---

## 第 13 章 高并发交易场景（秒杀 · 库存 · 下单）

> 本章所有题目都先给出业务场景与量化约束，再展开追问。答题时先复述约束、再给方案，是这一章的得分要点。

#### 13.1 【秒杀】100 万件库存、峰值 10 万 QPS，库存扣减怎么设计才不超卖？
> **难度**：L3 深入 ｜ **考察频次**：高 ｜ **目标能力**：架构权衡 ｜ **标签**：秒杀、库存扣减、分布式锁

**业务场景与约束**
- **业务背景**：电商大促单 SKU 限量秒杀，面向手机端普通消费者；超卖会直接产生资损并向客服爆发客诉；少卖最多损失部分 GMV，可接受。
- **量化约束**：单 SKU 100 万件库存；峰值 10 万 QPS；一致性要求为「零超卖、允许 0.5% 少卖」；下单 P99 < 300ms；基础设施为 Redis Cluster + MySQL 分库分表。

**渐进式追问**
1. 流量只有 100 QPS 时，单机能怎么保证扣减不超卖？
2. 流量放大到 10 万 QPS，直接让请求打进数据库 `UPDATE` 会崩在哪？
3. 为什么「先 `SELECT` 查库存再 `UPDATE` 扣减」在并发下一定超卖？
4. 既然超卖是因为并发，那加一把分布式锁为什么还是挡不住 10 万 QPS？
5. 用 Redis `Lua` 把库存扣减做成原子操作后，异步落库到 MySQL 失败了怎么办？
6. 业务允许 0.5% 少卖，那多扣（或扣了没卖出去）的库存怎么回收？
7. 若 Redis 与 MySQL 最终对不上，靠什么做最后兜底？

**参考答案要点**
主方案是「分层削峰 + Redis 原子扣减 + MQ 异步落库 + 数据库乐观锁兜底 + 对账补偿」五段式。削峰层先用答题/令牌桶/风控把 10 万 QPS 压到可接受量级；库存按段拆分到多个 `key`（如 `stock_00…stock_15`），用 `Lua` 脚本原子判断并扣减，避免单 `key` 热点；扣减成功后发 MQ 异步落库，MySQL 用 `UPDATE stock=stock-1 WHERE id=? AND stock>=1` 做乐观锁兜底，影响行数为 0 即视为已售罄。

为什么不选纯分布式锁：锁保护的是「单次操作互斥」，10 万 QPS 下要么锁竞争把 RT 打到秒级，要么退化成串行——锁解决不了量级，只解决正确性。为什么不选 `SELECT` 再 `UPDATE`：`SELECT` 读到的值在 `UPDATE` 前已被别的线程改掉，这是经典的不可重复读，必须让判断和扣减在同一行原子完成。

Redis 扣减成功但落库失败，靠「已扣不减回」+ 对账：Redis 是权威计数，落库失败只记失败流水，由定时任务补偿重试；少卖回收靠活动结束后的对账任务比对 Redis 与 MySQL，把未成交预占回收为可售。若 Redis 与 MySQL 严重偏离，以 MySQL 乐观锁结果为准——它才是零超卖的最后防线。

**可接受的关键结论**
- 零超卖的防线必须放在「判断与扣减原子」上，靠 `Lua` 或 `WHERE stock>=n` 实现，不能靠应用层 `SELECT+UPDATE`。
- 分布式锁解决正确性不解决量级，10 万 QPS 下它不是主方案。
- Redis 负责扛量、MySQL 乐观锁负责兜底，两者是「快路径 + 权威路径」关系。
- 落库失败不要回滚 Redis，用补偿对账回收少卖，保证零超卖优先于少卖回收。
- 活动结束后必须跑对账任务，否则 Redis 与 MySQL 的长期漂移无法自愈。

**考察意图**
这题验证候选人能不能从「写对一条 SQL」升级到「扛住一个量级的系统设计」。区分 L2 和 L3 的分水岭在：L2 会给出 Redis+锁的方案并认为安全；L3 能明确指出「锁挡不住量级」「扣减与判断必须原子」「落库失败用补偿而非回滚」这三句，并解释每一处的代价取舍。

**常见错误回答**
- ❌ 「加一把 `Redis` 分布式锁保证不超卖」—— 锁只互斥单次操作，10 万 QPS 下退化为串行或 RT 爆炸，挡不住量级。
- ❌ 「先查库存够不够，够就减」—— `SELECT` 与 `UPDATE` 之间有并发窗口，必然超卖，必须原子化。
- ❌ 「扣减成功落库失败就把 Redis 库存加回去」—— 回滚会引入超卖竞态，正确做法是记失败流水走补偿。
- ❌ 「用 `MySQL` 事务保证原子就行」—— 事务把单行锁住，10 万 QPS 直接把行锁和连接打满。
- ❌ 「库存不一致跑个定时任务就好了，不用设计」—— 没说清对账以谁为权威、少卖怎么回收，等于没兜底。

---

#### 13.2 【下单】库存该在下单时扣、支付时扣，还是先预占？
> **难度**：L1 入门 ｜ **考察频次**：高 ｜ **目标能力**：方案对比 ｜ **标签**：库存预占、下单、关单回滚

**业务场景与约束**
- **业务背景**：标准电商下单链路，用户从购物车提交订单；库存是稀缺资源，扣错时机要么丢单要么资损。
- **量化约束**：日均订单 50 万；支付转化率 70%；平均支付时长 3 分钟；下单后 15 分钟未支付自动关单释放库存。

**渐进式追问**
1. 下单时直接扣库存，会有什么问题？
2. 支付成功时才扣库存，用户下单后没库存了怎么办？
3. 什么叫「先预占、支付后确认、超时释放」的三态模型？
4. 三态模型里，15 分钟未支付怎么把库存放回去？
5. 关单回滚库存时重复关单会扣两次吗？怎么保证幂等？
6. 有人恶意下单占库存不付，怎么防？

**参考答案要点**
三种时机本质是「超卖风险 / 少卖风险 / 用户体验 / 实现复杂度」的取舍，给一张对比表：

| 时机 | 超卖风险 | 少卖风险 | 用户体验 | 复杂度 |
|------|---------|---------|---------|-------|
| 下单即扣 | 无 | 30% 未支付订单占着库存，等效少卖 | 好 | 低 |
| 支付即扣 | 高，支付瞬间库存可能已被占 | 无 | 差，易「下单成功却买不到」 | 低 |
| 先预占+确认 | 无 | 仅预占未支付这段窗口，少卖最小 | 好 | 中（需关单回滚） |

结论是选「预占」：下单时 `stock` 减库存、同时生成一条 `order` 状态为 `CREATED`；支付回调把状态置 `PAID` 即确认；关单任务在 15 分钟末把 `CREATED` 未支付单的库存加回。回滚必须幂等——用状态机 `CAS(CREATED→CLOSED)` 或乐观锁版本号，关单任务重复跑也只生效一次；恶意占库存靠单用户预占上限 + 风控降权。

**可接受的关键结论**
- 下单即扣体验好但有 30% 的「占而不付」少卖；支付即扣会超卖伤体验，一般不选。
- 预占三态（CREATED/PAID/CLOSED）是兼顾零超卖与少卖最小的折中。
- 关单回滚必须用状态机 CAS，不能先查状态再更新，否则重复关单会多放库存。
- 预占不是无限占，要配超时释放 + 单用户预占上限防恶意。

**考察意图**
这是本模块唯一的 L1 锚点，验证应届生是否理解「扣库存是一个带业务语义的状态问题」而非一句 SQL。分水岭在于：能不能张口就给出三态模型和那张对比表，并说出 70% 转化率下「下单即扣 = 30% 少卖」这种量化直觉。

**常见错误回答**
- ❌ 「支付时扣最安全，不会少卖」—— 忽略了支付瞬间别人已占库存导致的超卖和体验崩塌。
- ❌ 「下单即扣就行，简单」—— 没算过 30% 未支付订单等于把三成库存白白占 15 分钟。
- ❌ 「关单时查一下没支付就回库存」—— 先查后改有竞态，重复关单会多放，必须 CAS。
- ❌ 「库存回滚失败重试就行」—— 没说幂等，重试本身就是重复放库存的来源。
- ❌ 「预占不用释放，用户迟早会付」—— 真实场景有弃单和恶意占，不释放库存就锈死。

---

#### 13.3 【热点行】单 SKU 1 万 TPS 扣减，MySQL 行锁打满、CPU 飙升
> **难度**：L3 深入 ｜ **考察频次**：中 ｜ **目标能力**：瓶颈定位 ｜ **标签**：热点行、行锁、库存分桶

**业务场景与约束**
- **业务背景**：爆款单 SKU 被瞬时集中扣减，全表其他数据正常，只有这一行成为瓶颈。
- **量化约束**：MySQL 8.0；单表 2 亿行；热点集中在单行；事务 RT 从 5ms 涨到 300ms，CPU 打满。

**渐进式追问**
1. 明明只更新一行 `WHERE id=?`，为什么还会有锁等待和 CPU 飙升？
2. `UPDATE ... SET stock=stock-1 WHERE id=? AND stock>=1` 已经是行锁了，为什么仍然排队？
3. 把库存拆成 16 个子桶 `stock_00…stock_15` 随机路由，怎么扣、怎么合并？
4. 能不能用 Redis 前置把热点挡在数据库外面？
5. 批量合并提交（group commit）在这种场景怎么用？
6. 调 `innodb_thread_concurrency` 能解决吗？加索引或换隔离级别有用吗？

**参考答案要点**
先定位：单行热点下，所有 `UPDATE` 都要拿同一行的 `X` 锁，事务没提交前后来者只能排队；InnoDB 还要跑死锁检测（`innodb_deadlock_detect`），每笔都要和等待队列里的其他人比对，队列越长开销越大——RT 从 5ms 涨到 300ms、CPU 飙升正是这个排队 + 检测的开销，不是 SQL 慢。

注意「行锁」只保证互不覆盖，不保证不排队：1 万 TPS 抢同一行，等价于 1 万 个事务串行过这道门，单行就是天花板。解决思路按代价从低到高：① Redis 前置扣减，把 99% 请求挡在 DB 外，DB 只异步对账；② 库存分桶，逻辑库存拆 16 份 `stock_00…stock_15`，扣减时随机选桶 `WHERE bucket=? AND stock>=1`，把单行热点打散成 16 行，跨桶不足时再选第二个桶合并；③ group commit，把同桶短时间窗内的多次扣减合并成一次 `UPDATE`，降低单行写频率。

为什么加索引/换隔离级别基本没用：热点在「被更新那行」本身，索引只加速定位、隔离级别只影响读，都不减少写这一行的固有串行；`innodb_thread_concurrency` 限制并发进入 InnoDB 的线程数，调它能缓解线程风暴却换不来吞吐，是止血不是治病。

**可接受的关键结论**
- 行锁保证不互相覆盖，但不消除排队；单行热点本质是「写串行」，RT 与 CPU 暴涨来自队列 + 死锁检测开销。
- 根治靠把单行热点打散：Redis 前置 + 库存分桶（随机路由 + 跨桶合并）最有效。
- group commit 用「合并写」降低单行写频率，适合扣减可累积的场景。
- 加索引、换隔离级别、调 `innodb_thread_concurrency` 都治标不治本，别把时间花在这。

**考察意图**
验证候选人会不会「看到 CPU 高就无脑加索引/调参」。分水岭在：能否一针见血说出「行锁也会排队、死锁检测有开销、热点是写串行」，并给出分桶打散的具体实现，而不是甩一句「上缓存」。

**常见错误回答**
- ❌ 「加个索引让 `UPDATE` 更快」—— 热点在更新行本身，索引只帮定位，不减少写这一行的串行。
- ❌ 「把事务隔离级别调低就不锁了」—— 写永远要拿 X 锁，隔离级别只影响读，热点照旧。
- ❌ 「调大 `innodb_thread_concurrency` 提升并发」—— 这是限制进 InnoDB 的线程数，调大反而加剧排队风暴。
- ❌ 「直接上 Redis 就行，不用管 MySQL」—— 没说清 Redis 与 DB 如何对账、分桶合并怎么落地，等于甩名词。
- ❌ 「热点行用 `SELECT ... FOR UPDATE` 提前锁」—— 提前显式加锁只会把隐式排队变成长事务阻塞，更糟。
- ❌ 「分库分表就能解决」—— 单 SKU 无法按 id 再分，分表反而把热点集中到某分片的一行。

---

#### 13.4 【幂等】用户重复点击 + 网关重试导致重复下单
> **难度**：L2 进阶 ｜ **考察频次**：高 ｜ **目标能力**：边界处理 ｜ **标签**：幂等、令牌、唯一索引

**业务场景与约束**
- **业务背景**：弱网下用户狂点提交，叠加网关超时重试，同一笔意向订单可能被发多次。
- **量化约束**：网关超时重试最多 3 次；App 弱网可重发；订单表 5 亿行；下单峰值 3000 QPS。

**渐进式追问**
1. 为什么「先查有没有再插」不能保证不重复下单？
2. 幂等令牌怎么发、怎么校验、怎么消费？
3. 令牌校验和删除如果不是原子的会怎样？
4. 除了令牌，数据库还能做什么兜底？
5. 捕获 `DuplicateKeyException` 和「先查后插」哪个更可靠？
6. 为什么「加个分布式锁」不等于幂等？

**参考答案要点**
先查后插有竞态窗口：两个相同请求同时过 `SELECT`（都查到没有），再先后 `INSERT`，都成功——并发下「查无」不保证「插时仍无」。正确做法是把「唯一性」交给数据库：建唯一索引 `uk(user_id, item_id, token)`，重复插入直接抛 `DuplicateKeyException`，应用捕获后返回「已下单」而非报错。这比任何应用层判断都可靠，因为唯一索引的约束在存储引擎层原子生效。

幂等令牌链路：下单页先向 `POST /order/token` 领一个 `token`（写 Redis `SET token userId NX EX 300`）；提交时带 `token`，服务端用 `Lua` 原子「校验并删除」——`if redis.call('get',KEYS[1])==ARGV[1] then redis.call('del',KEYS[1]) return 1 else return 0 end`，删除与校验原子，避免「校验通过但删除前被第二个请求再次校验」的重放。令牌是防重放的第一道，唯一索引是兜底。状态机层面，订单从 `CREATED→PAID` 的流转也要幂等：用 `CAS` 或版本号，让重复支付回调只生效一次。

为什么分布式锁 ≠ 幂等：锁只保证同一时刻只有一个请求进入，但锁释放后、或不同 `token`、或重试落在不同节点，锁都不防；幂等要的是「相同输入重复执行结果一致」，靠的是唯一约束和状态机，不是互斥。

```java
if (redisTemplate.execute(tokenScript,
        List.of(tokenKey), userId) == 1L) {
    createOrder(userId, itemId, token); // 校验删除原子，重放被挡
}
```

**可接受的关键结论**
- 先查后插有并发竞态窗口，不能保证幂等；唯一索引 `uk(user_id,item_id,token)` 才是兜底。
- 幂等令牌的校验与删除必须原子（`Lua`），否则有重放窗口。
- 捕获 `DuplicateKeyException` 比「先查后插」可靠，因为约束在引擎层原子生效。
- 分布式锁防并发不防重复，幂等靠唯一约束 + 状态机 CAS，不是靠锁。

**考察意图**
验证候选人是否分得清「互斥」和「幂等」。分水岭在：能否说清先查后插的窗口、用唯一索引兜底、并且点破「加锁 ≠ 幂等」——很多候选人把锁当万能膏药，这一题专门拆它。

**常见错误回答**
- ❌ 「下单前先查有没有这个订单，没有再插」—— 并发下两请求同时查到「没有」，都插成功，锁不住。
- ❌ 「加分布式锁就幂等了」—— 锁只互斥同时刻，重试/换节点/不同 token 都不防。
- ❌ 「令牌校验后单独 `DEL` 删除」—— 校验与删除非原子，中间可被重放再来一次。
- ❌ 「重复下单就让用户自己取消」—— 没兜底，5 亿行里照样产生脏单和资损。
- ❌ 「捕获异常后重试一次」—— 没区分异常类型，重试正好又插一条，越重试越多。

---

#### 13.5 【关单】订单 15 分钟未支付自动关闭并回滚库存
> **难度**：L2 进阶 ｜ **考察频次**：中 ｜ **目标能力**：方案选型 ｜ **标签**：关单、延迟消息、状态机

**业务场景与约束**
- **业务背景**：下单预占库存后，用户迟迟不付，需要到点自动关单把库存和券放回去。
- **量化约束**：日均 50 万订单；峰值 2000 单/分钟待关闭；关单需回滚库存与优惠券。

**渐进式追问**
1. 用 RocketMQ 延迟消息、Redis ZSet 轮询、定时扫表、Kafka 时间轮，分别怎么实现？
2. 这几种方案在精度、可靠性、实现成本上怎么取舍？
3. 关单和支付回调同时到，怎么保证不出现「关了又付」或「付了又关」？
4. 定时扫表用深分页会慢 SQL，怎么避免？
5. 关单本身失败了怎么补偿重试？

**参考答案要点**
四种实现对比：

| 方案 | 精度 | 可靠性 | 成本 | 适用 |
|------|------|--------|------|------|
| RocketMQ 延迟消息（18 级等级） | 分钟级（等级跳变） | 高，消息不丢 | 低，开箱即用 | 有 RocketMQ 首选 |
| Redis ZSet 轮询 | 秒级 | 中，依赖 Redis 持久化 | 中 | 精度要求高 |
| 定时扫表分页 + 索引 | 取决于扫描间隔 | 高 | 中，需防慢 SQL | 无 MQ 时的兜底 |
| Kafka 时间轮 + 中转 topic | 秒级 | 高 | 高，Kafka 无原生延迟 | 已用 Kafka 且要精度 |

核心是「关单与支付回调的竞态」必须靠状态机，不能先查状态再更新：支付回调到达时 `UPDATE order SET status='PAID' WHERE id=? AND status='CREATED'`（CAS）；关单任务同样 `WHERE status='CREATED'`，两者谁先到谁把状态改成终态，后者 CAS 影响行数为 0 即视为已处理。绝不能用「先 `SELECT status` 判断再 `UPDATE`」——那是竞态重灾区。

扫表避免深分页：`WHERE status='CREATED' AND close_time<now() AND id>? ORDER BY id LIMIT 500` 用「上一页最大 id」游标翻页，走 `(status, close_time, id)` 联合索引，避免 `LIMIT 100000,500` 的回表爆炸。关单失败靠任务幂等重试：CAS 失败即跳过，成功则发回滚消息，回滚库存/券也各自幂等。Kafka 无原生延迟消息，用时间轮把到期消息投到中转 topic 再消费，或外接延迟组件。

**可接受的关键结论**
- 有 RocketMQ 优先用延迟消息，Kafka 场景用时间轮 + 中转 topic 模拟延迟。
- 关单与支付回调的竞态必须用状态机 CAS（`WHERE status='CREATED'`），先查后改必翻车。
- 定时扫表用「上一页最大 id」游标分页 + 联合索引，杜绝深分页慢 SQL。
- 关单与回滚都要幂等，CAS 影响行数为 0 即视为已处理，失败重试安全。

**考察意图**
验证候选人在「延时触发」这道题上会不会选型而不是背名词。分水岭在：能否给出四方案对比表，并明确指出「关单和支付回调的竞态只能靠状态机 CAS，先查后改是坑」——这一句区分了做过和没做过。

**常见错误回答**
- ❌ 「关单前先查订单状态，是 CREATED 就关」—— 查与改之间有窗口，支付回调同时到就会「关了又付」。
- ❌ 「用 `LIMIT 100000,500` 扫表，简单」—— 深分页回表爆炸，越扫越慢，必须游标分页。
- ❌ 「Kafka 也有延迟消息，直接用」—— Kafka 无原生延迟消息（4.0 仅 KRaft），要时间轮或外接组件。
- ❌ 「关单失败不管，下次再扫」—— 没说幂等，回滚库存/券重复执行会多放。
- ❌ 「关单任务用分布式锁保证只跑一次」—— 锁防并发不防「跑一半挂了」，CAS 幂等才靠谱。

---

#### 13.6 【线程模型】秒杀下单服务线程池怎么配？虚拟线程能直接上吗？
> **难度**：L3 深入 ｜ **考察频次**：中 ｜ **目标能力**：容量估算 ｜ **标签**：线程池、虚拟线程、容量估算

**业务场景与约束**
- **业务背景**：秒杀下单服务是 IO 密集型，但代码里混了少量 CPU 密集逻辑（加解密/风控计算）。
- **量化约束**：容器 8C16G；下游平均 RT 50ms；下单峰值 8000 QPS；JDK 21/25（Java 25 为 2025-09 LTS，Java 21 存量主流）。

**渐进式追问**
1. IO 密集场景下，核心线程数大概按什么公式估算？
2. 队列为什么不能用无界队列？拒绝策略怎么选？
3. 为什么不应直接 `Executors.newFixedThreadPool`？
4. Tomcat 线程池和业务线程池为什么要隔离？
5. 虚拟线程（Java 21 转正，JEP 444）适不适合这里？什么情况反而不适合？
6. synchronized pinning 在 JDK 24（JEP 491）才解决，对上虚拟线程意味着什么？

**参考答案要点**
IO 密集容量估算：核心线程数 ≈ 期望并发 = QPS × 平均 RT = 8000 × 0.05s = 400 个并发任务。若下游 RT 50ms 且大部分时间在等网络，平台线程池配核心 400、最大 400~600，`keepAlive` 短一点即可。队列绝不能用无界：`LinkedBlockingQueue` 无界会在下游抖动时无限堆积，把内存和 P99 拖爆；应使用有界队列（如 200~500）配 `CallerRunsPolicy`——满了让调用方自己跑，既限流又保护下游。

为什么不用 `Executors.newFixedThreadPool`：它默认无界队列 + 固定线程，下游一抖就堆积到 OOM，且线程数写死不灵活。`Tomcat` 线程池（处理 HTTP）和业务线程池（跑下单逻辑）要隔离：否则一个慢下游把 Tomcat 线程占满，连带健康接口也 503，故障扩散。

虚拟线程：Java 21 转正（JEP 444），适合「大量阻塞 IO、少量计算」——它用少量载体线程承载海量虚拟线程，吞吐量高、写起来像同步代码。但不直接无脑上：① 若真瓶颈是连接池（DB/Redis 连接数固定），虚拟线程再多也挤在等连接，吞吐不增；② CPU 密集逻辑混在里面会长期占住载体线程，抵消优势；③ `synchronized` 在虚拟线程上会 pinning（把载体线程钉死），直到 JDK 24（JEP 491）才解决，JDK 21/25 上要改用 `ReentrantLock`。引入姿势：先把同步阻塞调用迁到虚拟线程 + 用 `ReentrantLock` 替代 `synchronized`，并确认下游是 IO 而非连接池瓶颈，再灰度。

**可接受的关键结论**
- IO 密集核心线程数 ≈ QPS × 平均 RT（本题约 400），队列必须有界，拒绝策略用 `CallerRunsPolicy` 限流。
- 别用 `newFixedThreadPool`：无界队列 + 抖动 = OOM；Tomcat 与业务线程池必须隔离防故障扩散。
- 虚拟线程适合阻塞 IO 多的场景，但连接池才是真瓶颈时上了也没用。
- 虚拟线程 + `synchronized` 在 JDK 21/25 会 pinning，JEP 491（JDK 24）才解决，要改用 `ReentrantLock`。

**考察意图**
验证候选人会不会「按容量算线程数」而不是拍脑袋。分水岭在：能否给出 8000×0.05≈400 的估算、点破无界队列的 OOM 风险，并说清虚拟线程的适用边界与 pinning 这个具体坑——背过名词的人说不出 JEP 491。

**常见错误回答**
- ❌ 「线程数设成 CPU 核数 × 2 就行」—— 那是 CPU 密集公式，IO 密集要按 QPS×RT 估算，本题约 400。
- ❌ 「用 `newFixedThreadPool` 省事」—— 无界队列，下游抖一下就堆积到 OOM。
- ❌ 「队列设大一点更稳」—— 无界/超大队列把 RT 和内存拖爆，限流能力归零。
- ❌ 「上虚拟线程就完事，不用管线程池」—— 连接池瓶颈 + CPU 密集混算下，虚拟线程毫无收益甚至更差。
- ❌ 「虚拟线程里随便用 `synchronized`」—— JDK 21/25 会 pinning 钉死载体线程，得换 `ReentrantLock`（JEP 491 才解）。
- ❌ 「Tomcat 和业务共用一个线程池简单」—— 慢下游占满 Tomcat 线程，健康接口也跟着 503。

---

#### 13.7 【RT 抖动】下单接口 P99 从 50ms 涨到 800ms，但 CPU 和 DB 都正常
> **难度**：L3 深入 ｜ **考察频次**：中 ｜ **目标能力**：排障思路 ｜ **标签**：RT 抖动、长尾、链路追踪

**业务场景与约束**
- **业务背景**：下单接口白天偶发长尾超时，但监控上 CPU、DB 都「正常」，均值也正常。
- **量化约束**：仅 10% 采样日志 + 监控面板；平均 RT 仍 55ms；超时率 0.3%；集中在 20:00–22:00 高峰。

**渐进式追问**
1. 平均值 55ms 但 P99 800ms，这本身说明了什么？
2. 怎么用链路追踪定位到具体慢在哪一段（span）？
3. GC 停顿会不会是元凶？该怎么查而不是瞎猜？
4. 线程池队列在等，为什么看 CPU 是「正常」的？
5. `CompletableFuture` 默认共用 `ForkJoinPool` 会带来什么坑？
6. 为什么「CPU 正常」就排除 JVM 问题是错的？

**参考答案要点**
均值正常、P99 暴涨 = 长尾：少数请求极慢把分位拉高，但被大量快请求平均掉了，所以看均值会误判。第一步用链路追踪按 P99 采样（而非全量或按均值），把慢请求的具体 span 拉出来——是「调用下游」「查 DB」还是「线程等待」，一眼定位，而不是盯大盘。

常见真凶按优先级：① 线程池队列等待——请求在队列里排长队，线程本身空闲、CPU 当然「正常」，必须看队列长度而非 CPU；② GC 停顿——用 `-Xlog:gc*`（JDK 9+ 语法，别再用 `-XX:+PrintGCDetails`）看 `Pause` 时长，长尾往往对应一次上百毫秒的 Full GC/大对象分配；③ 连接池等待——HikariCP/Druid 连接借不到在等，监控只显示「DB 正常」；④ `CompletableFuture` 默认跑在公共 `ForkJoinPool.commonPool()`，一旦某处慢任务占满，所有用默认池的异步逻辑互相拖累，长尾被放大——应自定义线程池；⑤ 下游 50ms 抖动在串行调用里被乘成 800ms。

为什么「CPU 正常就排除 JVM」错：卡在锁、队列、GC 安全点、连接等待时，线程是 blocked/waiting，不消耗 CPU，CPU 自然低；JVM 的停顿类问题恰恰表现为「CPU 低但 RT 高」。排障要按 trace 而非按 CPU。

**可接受的关键结论**
- 均值正常 P99 高 = 长尾，被大量快请求平均掩盖，必须按 P99 采样看 trace 而非盯均值。
- 卡在队列/锁/GC/连接等待时线程是 waiting，CPU 反而低，「CPU 正常」不能排除 JVM 问题。
- GC 用 `-Xlog:gc*` 看停顿，线程池要看队列长度，连接池要看等待次数。
- `CompletableFuture` 别用默认 `ForkJoinPool`，自定义线程池避免互相拖累。

**考察意图**
验证候选人有没有「系统性排障」的肌肉记忆。分水岭在：能否从「均值正常但 P99 高」推出长尾、并指出「CPU 正常恰恰说明线程在等」这个反直觉点，而不是无脑说「加机器/优化 SQL」。

**常见错误回答**
- ❌ 「平均才 55ms 说明没问题，不用管」—— P99 800ms 是真实长尾超时，被均值掩盖了。
- ❌ 「用 `-XX:+PrintGCDetails` 看 GC」—— JDK 9+ 已废弃，应统一用 `-Xlog:gc*`。
- ❌ 「CPU 不高就说明不是 JVM 的问题」—— 等锁/等队列/GC 安全点时线程 waiting，CPU 本就低。
- ❌ 「线程池配大点就好了」—— 没先看队列长度，盲目加线程只会加剧上下文切换和下游压力。
- ❌ 「直接 `jstack` 看哪个线程忙」—— 长尾时线程多在等，该看 trace 的慢 span 和队列/池等待，不是看谁忙。
- ❌ 「`CompletableFuture` 用默认池方便」—— 公共池被一处慢任务占满，所有异步逻辑被拖成长尾。

---

#### 13.8 【防刷限流】黄牛脚本 5 万 QPS 打下单接口
> **难度**：L2 进阶 ｜ **考察频次**：高 ｜ **目标能力**：分层防御 ｜ **标签**：限流、防刷、熔断降级

**业务场景与约束**
- **业务背景**：黄牛用脚本刷下单接口抢货转卖，真实用户被挤在门外。
- **量化约束**：正常用户峰值 8000 QPS；黄牛脚本峰值 5 万 QPS；单机 500 QPS 承载能力；要尽力放行真实用户。

**渐进式追问**
1. 只在一个地方限流够吗？应该分哪几层？
2. 令牌桶、漏桶、滑动窗口分别适合哪一层？
3. 用 Redis + Lua 做集群限流，单机限流在集群下会放大多少？
4. 限流之外的风控还能做什么？
5. 下游快扛不住时，熔断和降级怎么配合快速失败？

**参考答案要点**
单一限流必漏，要分层：① 网关层按「总 QPS / 单 IP / 单用户」限流，把 5 万 QPS 在入口砍到接近 8000；② 接口维度限流，保护下单自身；③ 库存维度限流，库存剩多少就放多少请求进扣减；④ 业务维度用令牌桶做「匀速放行」，平滑突发。

算法选型：网关入口用令牌桶（允许短时突发、平滑），需刚性限速用漏桶（恒定速率、削峰），需要更准的瞬时计数用滑动窗口（比固定窗口少一倍临界突刺）。集群限流用 Redis + Lua 原子计数（`INCR+EXPIRE` 或 `ZSet` 时间窗），但单机限流在 N 台机器下总配额被放大 N 倍——若单机限 500，10 台集群实际放 5000，必须改成「中心化 Redis 计数 / 或单机配额 = 总配额/N」并留余量。

限流只是第一道，黄牛会换 IP/账号绕过，必须叠风控：设备指纹（同设备多账号）、行为验证（答题/滑块）、历史黑名单、下单频次异常检测。下游抖动时熔断（如基于错误率/RT 的熔断器）打开，降级为「排队中/稍后重试」快速失败，避免线程被黄牛打满拖垮真实用户。预热冷启动：活动开始瞬间逐步放开配额，防瞬时击穿。

**可接受的关键结论**
- 限流要分层：网关 QPS → 用户 → 接口 → 库存，单层必漏。
- 集群下单机限流总配额被放大 N 倍，必须中心化 Redis 计数或配额按 N 均分。
- 令牌桶/漏桶/滑动窗口按「要突发/要刚性/要精准」选层，不是万能一个。
- 限流挡不住换 IP 黄牛，必须叠设备指纹/行为验证/黑名单；下游快崩时用熔断降级快速失败。

**考察意图**
验证候选人会不会「立体防御」而不是堆一个限流中间件。分水岭在：能否指出集群下单机限流的 N 倍放大误差、并自然带出风控与熔断降级——只会配一个 `sentinel` 规则的人答不到这层。

**常见错误回答**
- ❌ 「在接口里加个限流注解就行」—— 单层限流，黄牛换 IP/账号就绕过，真实用户仍被挤。
- ❌ 「每台机器限 500 QPS 就够了」—— 10 台集群实际放 5000，总配额被放大 N 倍，必须中心化。
- ❌ 「限流配大点别误杀真实用户」—— 不分层不风控，大配额直接被黄牛 5 万吃满。
- ❌ 「限流用漏桶最准」—— 漏桶削峰但无突发，入口用令牌桶更贴合「放行真实突发」的诉求。
- ❌ 「黄牛靠限流就防住了」—— 没设备指纹/行为验证，黄牛换 IP 秒破。
- ❌ 「下游慢就加机器」—— 不熔断降级，黄牛把线程打满，加机器也被冲垮。

---

## 第 14 章 资金 · 对账 · 营销发放场景

> 本章聚焦"钱"的场景：任何一笔账错了都要能追回来。答题时先复述约束与一致性要求，再给方案。

#### 14.1 【支付回调】渠道重复回调、回调丢失、回调乱序，怎么保证最终一致？
> **难度**：L3 深入 ｜ **考察频次**：高 ｜ **目标能力**：分布式一致性 ｜ **标签**：支付回调、幂等、最终一致

**业务场景与约束**
- **业务背景**：接入 3 个支付渠道（微信、支付宝、银联），为电商订单提供收单能力；财务、用户、商户三方都依赖支付结果推进后续流程；回调处理错一次，轻则用户付了钱没发货，重则重复出货或资损。
- **量化约束**：日均 80 万支付单；回调峰值 3000 QPS；渠道账单 T+1 09:00 才到；允许 5 分钟内最终一致；单渠道回调重复率约 1.5%。

**渐进式追问**
1. 单机单渠道下，你怎么保证同一笔支付回调被重复投递时只生效一次？
2. 状态机是幂等的核心，你会用订单状态机的 CAS 还是单独建一张去重表？为什么？
3. 回调乱序（比如支付成功回调比支付中回调晚到）时，怎么防止状态被错误回退？
4. 上了 3 个渠道、峰值 3000 QPS 后，去重和状态判断的并发压力怎么化解？
5. 渠道回调丢了（用户已付款但没收到回调）怎么办，靠什么兜底？
6. 主动查单补偿和回调同时到达同一笔单，两者并发写状态怎么不打架？
7. 回调处理里还要做金额和签名校验，为什么校验必须放在落库之前，失败怎么保证不丢消息？

**参考答案要点**
- 主方案：以订单状态机为幂等核心，所有状态流转走单条条件更新（CAS）。成功回调把 `PAYING→PAID`，重复回调因 `WHERE status=PAID` 不成立而 0 行生效，天然幂等；**不去建单独的去重表**，它只是状态机之外的冗余，反而要额外维护一致性。

```java
int n = jdbc.update(
  "UPDATE orders SET status=?, version=version+1 " +
  "WHERE id=? AND status=? AND version=?",
  PAID, id, PAYING, oldVer);
if (n == 0) return ALREADY_DONE; // 幂等：重复回调直接返回
```

- 乱序：非法流转被状态机拒绝（如收到 `PAYING` 但当前已是 `PAID`），配合 `version` 版本号防止 ABA 回退。
- 丢失兜底：定时任务按 `gmt_create` 扫描 5 分钟内仍 `PAYING` 的订单，调渠道主动查单接口补状态；再外层 T+1 渠道账单对账兜底，发现单边账自动补单。
- 并发：回调与主动查单对同一订单抢同一把分布式锁（如 `Redis` `SET NX` 订单维度），拿不到锁的直接跳过、由另一路径补偿；拿到锁后再次走状态机 CAS 做双保险。
- 重试：消费失败用指数退避（1s/2s/4s…）且设最大 8 次，超过进死信人工处理；金额/签名校验在落表前完成，校验不通过的回调原样落 `callback_fail` 表并告警，绝不静默丢弃。

| 方案 | 一致性 | 实现成本 | 适用 |
|---|---|---|---|
| 状态机 CAS | 强 | 低 | 首选 |
| 去重表 | 中 | 中 | 不推荐单独用 |
| 账单 T+1 对账 | 最终 | 低 | 兜底 |

**可接受的关键结论**
- 幂等的核心必须是订单状态机的 CAS，而不是一张去重表。
- 乱序靠"状态机拒绝非法流转 + `version` 版本号"双保险。
- 回调丢失不能只靠渠道，必须主动查单 + T+1 对账两层兜底。
- 回调与主动查单并发要抢同一把订单锁，再加状态机兜底。
- 金额和签名校验必须在落库前完成，失败的回调要落失败表而非丢弃。

**考察意图**
验证候选人在分布式一致性里能不能区分"幂等"的两种实现层次：低分答案会说"建去重表 / 用 `Redis` 存已处理 ID"；分水岭在于是否理解去重表只是旁路、真正兜底是状态机的 CAS——面试官想听的就是"为什么不用去重表而用状态机"。

**常见错误回答**
- ❌ "用 `Redis` 存已处理的回调 `notify_id`，存在就跳过" —— 错在把幂等寄托在旁路缓存，缓存失效即资损；正确是以订单状态机 CAS 为唯一真相。
- ❌ "收到回调先 `SELECT` 看状态再 `UPDATE`" —— 错在读写之间有并发窗口会重复处理；正确是单条条件 `UPDATE` 原子完成。
- ❌ "乱序就按时间戳取最大的" —— 错在忽略状态合法性，可能把已退款的单回退成已支付；正确是状态机拒绝非法流转。
- ❌ "回调丢了等用户投诉再处理" —— 错在被动；正确是定时主动查单 + T+1 对账主动兜底。
- ❌ "校验失败直接 `return` 不处理" —— 错在静默丢消息导致单边账；正确是落失败表 + 告警 + 补偿。
- ❌ "重试一直重试不限次数" —— 错在可能压垮下游或造成重复；正确是指数退避 + 最大次数 + 死信。

---

#### 14.2 【账户余额】并发扣减与热点账户
> **难度**：L3 深入 ｜ **考察频次**：高 ｜ **目标能力**：并发正确性 ｜ **标签**：账户余额、并发扣减、热点账户

**业务场景与约束**
- **业务背景**：用户账户与商户结算账户都在同一张 `account` 表；任何一笔扣减都要有对应的流水，财务审计要求每分钱可追溯；余额算错会直接导致用户或商户资损。
- **量化约束**：`account` 表 8000 万行；单个大商户账户峰值 3000 TPS；余额绝不允许为负；每笔变动必须落流水；日终对账要求零差异。

**渐进式追问**
1. 你为什么不能用"`SELECT balance` 再 `UPDATE`"的方式扣减余额？
2. 单账户扣减你用哪条 SQL 保证原子且不为负？
3. 高并发下 CAS 失败（余额被别人改了）怎么办，重试次数和退避怎么定？
4. 单个大商户账户 3000 TPS 成为热点，单条 `UPDATE` 行锁扛不住，怎么拆？
5. 子账户拆分后，查询余额和跨子账户消费怎么汇总与归集？
6. 流水表和余额表怎么保证"扣了余额一定有条流水"，异常时怎么兜底？
7. 异步入账（先记流水后异步改余额）能不能用，什么场景才敢用？

**参考答案要点**
- 原子扣减：`UPDATE account SET balance = balance - ? WHERE id = ? AND balance >= ?`。数据库在行上施加排他锁一次性完成"判断 + 扣减"，`WHERE balance >= ?` 直接杜绝负数，比任何应用层判断都可靠。
- 为什么 `SELECT` 再 `UPDATE` 必错：两次操作之间有时间窗口，两个事务都读到 100、都扣 80，结果变成 20 而不是 -60 且都成功——典型超扣。即使加 `SELECT ... FOR UPDATE` 把锁提前到读阶段，也只是把热点行锁持有时间拉长，反而加剧热点。
- 热点账户：把大商户拆成 N 个子账户（如 16 个），扣减时按 `hash(userId) % 16` 随机路由到一个子账户，行锁被分散到 16 把；查询展示余额时对子账户求和，日终定时"归集"把子账户余额汇回主账户。
- 流水一致：流水表与余额表放在同一库，用本地事务保证 "`INSERT` 流水 + `UPDATE` 余额"同时成功或同时失败；异步入账只在对账可兜底的场景（如积分）使用，资金类必须同步同事务。
- 兜底：日终跑对账，按 `(account_id, biz_no)` 比对流水汇总额与余额，差异自动冲正或挂账。

**可接受的关键结论**
- 扣减必须单条 `UPDATE ... WHERE balance >= ?` 原子完成，不能先查再改。
- 热点账户用子账户水平拆分分散行锁，而非加更大锁。
- 流水与余额同一库本地事务，保证可追溯。
- 资金变动不同步异步入账，异步只用于可兜底的非资金场景。
- 日终对账是最后兜底，按业务单号比对流水与余额。

**考察意图**
考并发正确性里"数据库行锁 vs 应用层判断"的认知。L2 会说"加锁 / 用乐观锁版本号"但说不清为什么 `SELECT` 再改会超扣；L3 分水岭是能讲出"单条条件 `UPDATE` 在数据库内原子完成、应用层两段式必然有窗口"，并能针对 3000 TPS 热点给出子账户拆分方案。

**常见错误回答**
- ❌ "`SELECT balance` 后判断够不够再 `UPDATE`" —— 错在并发窗口会超扣；正确是单条 `UPDATE ... WHERE balance >= ?`。
- ❌ "热点账户加表级锁解决并发" —— 错在把并发能力打到零；正确是拆子账户分散行锁。
- ❌ "乐观锁版本号无限重试" —— 错在热点下重试风暴；正确是限定重试次数 + 退避，必要时走子账户拆分。
- ❌ "流水异步落，先改余额" —— 错在余额改了流水没落就不可追溯；正确是同事务同步落。
- ❌ "余额不对明天再对账就行" —— 错在把资金一致性当可延后；正确是本地事务即时一致 + 日终兜底。
- ❌ "负余额靠业务代码拦" —— 错在并发下拦不住；正确是 `WHERE balance >= ?` 由数据库兜底。

---

#### 14.3 【优惠券】1000 万张券大促首发
> **难度**：L3 深入 ｜ **考察频次**：高 ｜ **目标能力**：容量与防超发 ｜ **标签**：优惠券、防超发、大促

**业务场景与约束**
- **业务背景**：大促首发投放 1000 万张券，用户领券后用于下单抵扣；超发（多发了券）等于直接资损，少发（没发够）影响活动效果；运营要求发放可观测可回捞。
- **量化约束**：1000 万张券；领取峰值 5 万 QPS；每人每活动限领 1 张；库存按 10 个渠道分片；活动时长 2 小时。

**渐进式追问**
1. 券码你选预生成还是实时生成，各自代价是什么？
2. 5 万 QPS 下你怎么保证库存扣减不超发？
3. 库存存在 `Redis`，扣减和判"每人限领 1 张"怎么一起原子做？
4. 为什么不能靠 `SELECT count(*)` 判断还能不能领？
5. 发券成功后落库失败、或落库成功发券消息失败，怎么保证最终一致？
6. 发放量、核销量、库存三者的对账怎么发现超发或漏发？
7. 薅羊毛（黄牛批量领）你怎么在发放环节拦？

**参考答案要点**
- 券码取舍：预生成 1000 万个码提前落 `coupon_code` 表，领取时按 `status` 抢占分配，发放路径快但一次性占用存储且有"码泄露"风险（库里已生成未领的码若被拖库即失效）；实时生成（如雪花 `id` + 活动前缀）无存储压力但高并发下要保证全局唯一。大促首选预生成 + 领取时抢占，配合分片降低单点压力。
- 库存防超发：`Redis` 按 10 个渠道分片，每片 `DECR` 一个 `stock` 计数；扣减与"限领"用 `Lua` 脚本在一个 `eval` 里原子完成——脚本内先判用户是否已领，未领则 `DECR stock` 并返回成功，否则返回已领。`Lua` 在 `Redis` 单线程内执行，杜绝竞态。

```lua
if redis.call('HEXISTS', KEYS[1], ARGV[1]) == 1 then return -1 end
if redis.call('DECR', KEYS[2]) < 0 then redis.call('INCR', KEYS[2]); return 0 end
redis.call('HSET', KEYS[1], ARGV[1], 1); return 1
```

- 为什么不能 `SELECT count`：`count(*)` 是大表全扫且非原子，5 万 QPS 下两个请求同时看到"还能领"就会双发，且性能根本扛不住；限领靠 `uk(user_id, activity_id)` 唯一索引，插入冲突即"已领"，而非先查后插。
- 最终一致：发券成功后写 `MySQL` 发券记录，再投 `MQ` 异步通知下游；落库失败则整体回滚不发；`MQ` 消费失败重试 + 死信，库存已在 `Redis` 扣，账靠"发放量 vs 核销量 vs 库存"对账回捞。
- 风控：设备指纹 + 黑名单 + 单设备领券频次限流，在 `Lua` 扣减前拦截。

**可接受的关键结论**
- 库存扣减与限领必须在 `Redis` `Lua` 里原子完成，不能拆成两步。
- 限领靠 `uk(user_id, activity_id)` 唯一索引，不是 `SELECT count`。
- 券码预生成快但有泄露风险，实时生成无存储压力但要保唯一。
- 超发/少发靠"发放量、核销量、库存"三者对账发现。
- 风控（设备指纹/黑名单/频次）要在扣减前拦截。

**考察意图**
考高并发下的容量与防超发工程权衡。L2 会答"`Redis` 扣库存 + 数据库唯一索引"但讲不清为什么 `count` 不行、为什么两步会超发；L3 分水岭是能用 `Lua` 把"判库存 + 判限领 + 扣减"合成单原子操作，并主动谈预生成券码的泄露代价与对账回捞。

**常见错误回答**
- ❌ "先 `SELECT count` 看领没领再插" —— 错在并发下双发且大表慢；正确是唯一索引冲突即已领。
- ❌ "库存扣减和限领分两个 `Redis` 命令" —— 错在两条命令间有竞态会超发；正确是 `Lua` 单脚本原子。
- ❌ "券码实时生成更省事随便用" —— 错在忽略高并发唯一性保障成本；正确是权衡预生成泄露风险。
- ❌ "发券消息丢了不管" —— 错在造成漏发且对账对不上；正确是重试 + 死信 + 对账回捞。
- ❌ "防薅靠前端按钮置灰" —— 错在接口层毫无防护；正确是设备指纹 + 黑名单 + 频次。
- ❌ "超发一点没事" —— 错在等于直接资损；正确是必须零超发且可回捞。

---

#### 14.4 【每日对账】发现 372 笔差异怎么定位与处理
> **难度**：L3 深入 ｜ **考察频次**：中 ｜ **目标能力**：问题定位 ｜ **标签**：每日对账、差异定位、单边账

**业务场景与约束**
- **业务背景**：每天把本地支付流水和渠道 T+1 账单比对，任何差异都要在当天处置，否则财务关账后无法更正，影响对账报表和资金结算。
- **量化约束**：日 80 万单；渠道账单 T+1 09:00 到；差异率 0.05%（即约 372 笔）；差异必须在当天 18:00 前处理完；单笔差异定位耗时要求 < 2 分钟。

**渐进式追问**
1. 对账要比较哪三要素，缺一个会漏掉哪类问题？
2. 372 笔差异你怎么分类，长款、短款、错账、单边账各自根因是什么？
3. 常见根因里"跨日时区切分不一致"是怎么造成差异的？
4. 自动补单、自动冲正、人工挂账三种处理策略，各自什么准入条件？
5. 对账任务怎么保证可重跑且幂等，不会越跑越错？
6. 80 万单一次跑不完，怎么分片 + 断点续跑？
7. 退款单混入对账时，漏同步退款会造成哪种差异？

**参考答案要点**
- 三要素：笔数（订单数对得上吗）、金额（每笔钱对得上吗）、状态（成功/失败/退款一致吗）。只比笔数发现不了金额错；只比金额发现不了漏单。
- 差异分类与根因：长款（渠道有本地无，如回调丢失未落库）、短款（本地有渠道无，如测试单混入/跨日切分丢单）、错账（两边都有但金额或状态不一致，如金额精度、退款未同步）、单边账（只有一侧有记录，最危险）。常见根因清单：回调丢失、重复入账、金额精度（分 vs 元）、跨日时区切分不一致（本地按 `GMT+8` 00:00、渠道按 `UTC` 致边界单漂移）、退款未同步、测试/压测单混入生产。
- 处理策略准入：自动补单（仅限"渠道成功本地缺失"且金额可校验）→ 自动冲正（仅限"本地多记且确认渠道无"）→ 人工挂账（金额对不上、状态异常、无法自动判定）。三类必须按规则准入，禁止无脑自动。
- 对账可重跑：以 `(biz_date, channel, shard)` 为幂等键，重跑先清当日差异中间表再算；断点续跑按分片记录进度，失败分片重跑不影响已完成分片。

**可接受的关键结论**
- 对账必比笔数、金额、状态三要素，缺一漏一类问题。
- 差异先分类（长/短/错/单边）再按根因清单定位，不盲目处理。
- 自动补单/冲正有严格准入，无法自动判定的必须人工挂账。
- 对账任务以 `(biz_date, channel, shard)` 为幂等键，必须可重跑。
- 跨日时区切分不一致是高频根因，本地与渠道须统一时间基准。

**考察意图**
考线上问题定位的系统性。L2 会"逐笔 `diff` 看哪不对"但无分类框架、重跑会重复挂账；L3 分水岭是能建立"三要素 + 四分类 + 根因清单 + 处理准入"的方法论，并强调对账任务的可重跑幂等——面试官想听"372 笔不是 372 次手工，而是先分类批处理"。

**常见错误回答**
- ❌ "把本地和渠道文件逐行 `diff`" —— 错在无分类框架、慢且易漏金额类差异；正确是三要素结构化比对 + 分类。
- ❌ "差异一律自动补单" —— 错在把短款/错账也错补造成二次资损；正确是按准入条件区分。
- ❌ "对账任务跑一半重跑从头来" —— 错在无断点会重复挂账；正确是分片进度 + 幂等键。
- ❌ "测试单没隔离直接进生产对账" —— 错在制造假短款；正确是生产/测试单打标过滤。
- ❌ "时区各按各的" —— 错在跨日边界单漂移成单边账；正确是统一时间基准切分。
- ❌ "372 笔全人工处理" —— 错在 18:00 前处理不完；正确是分类后自动处置可自动的。

---

#### 14.5 【分布式事务】下单要同时扣库存、扣余额、发券
> **难度**：L3 深入 ｜ **考察频次**：高 ｜ **目标能力**：方案权衡 ｜ **标签**：分布式事务、TCC、最终一致

**业务场景与约束**
- **业务背景**：下单要同时扣库存、扣余额、发券，三个服务各持独立库；任何一步成功另一步失败都会出现"扣了库存没扣钱"的不一致，用户白拿或商户资损。
- **量化约束**：3 个服务各自独立库；端到端 RT 预算 300ms；允许秒级短暂不一致；但绝不允许"扣库存成功却没扣钱"的最终态；高峰期下单 1 万 TPS。

**渐进式追问**
1. 为什么在三个独立库上用 `@Transactional` 一定无效？
2. XA/2PC 为什么不适合你 300ms 的 RT 预算？
3. TCC 的 Try/Confirm/Cancel 三阶段各自做什么，怎么保证幂等？
4. TCC 的"空回滚"和"防悬挂"具体指什么，怎么实现？
5. Saga 的正向 + 补偿模式，补偿失败怎么办？
6. RocketMQ 事务消息（半消息 + 回查）怎么串起三个服务？
7. 为什么业务上最终你倾向"最终一致 + 对账兜底"而不是强一致？

**参考答案要点**
- `@Transactional` 无效：它只管单数据源的本地事务，跨库跨服务根本没有同一连接和资源管理器，第二个服务抛错第一个已提交，无法回滚。还要注意同类内部调用 `@Transactional` 失效、非 public 方法失效、异常被 `catch` 不回滚这些边界。
- XA/2PC 代价：所有参与者在 `Prepare` 阶段持有锁直到 `Commit`，链路最长=最慢节点，300ms 预算下锁持有过久、吞吐崩塌，且协调者单点，不适合高并发下单。
- TCC：Try 预留资源（库存冻结、余额冻结、券锁定），均不真正扣；Confirm 真正提交（全部成功才调用）；Cancel 释放预留（任一失败则全部回滚）。三阶段都要幂等（同 `tx_id` 重复调用结果一致）；空回滚=Try 未执行却收到 Cancel（如网络丢 Try），Cancel 要能识别并记空回；防悬挂=Try 在 Cancel 之后才到，Try 必须拒绝已空回的事务，否则资源永久占用。
- Saga：把长事务拆成一系列本地事务，每步有对应补偿；补偿也可能失败，需重试 + 告警 + 人工。
- 事务消息：`RocketMQ` 半消息先发，本地事务（扣库存）成功再 `Commit` 才被消费方看到，失败发 `Rollback`；Broker 未收到确认会回查本地事务状态。
- 选型：强一致代价高且易成瓶颈，业务可接受秒级短暂不一致时，优先"各服务本地事务 + `MQ`/`TCC` 推进 + 日终对账兜底"，用对账兜住极小概率的最终不一致；引入 Seata 要评估框架与运维成本。

**可接受的关键结论**
- 跨库 `@Transactional` 无效，必须分布式方案。
- XA/2PC 锁持有久，不满足 300ms 预算，不首选。
- TCC 要补齐幂等 + 空回滚 + 防悬挂三件套才稳。
- Saga 补偿可能失败，需重试 + 人工兜底。
- 业务可接受秒级不一致时，优先最终一致 + 对账兜底。

**考察意图**
考方案权衡而非背概念。L2 会罗列"TCC/Saga/Seata"名词；L3 分水岭是能针对 300ms RT 和"不允许扣库存没扣钱"的硬约束，排出"XA 因锁太重被否 → TCC 因要三件套成本高 → 最终选最终一致 + 对账兜底"的决策链，并点出 Seata 的引入运维成本。

**常见错误回答**
- ❌ "三个服务包一个 `@Transactional` 就行" —— 错在跨库无同一事务；正确是本地事务 + 分布式协调。
- ❌ "用 2PC 保证强一致" —— 错在锁持有超 300ms 预算；正确是权衡后弃用。
- ❌ "TCC 只写 Try/Confirm/Cancel 不管幂等" —— 错在重试会重复扣；正确是三阶段都幂等。
- ❌ "Cancel 收到就直接释放" —— 错在忽略空回滚/悬挂；正确是识别空回滚、防悬挂。
- ❌ "Saga 补偿失败重试几次就好" —— 错在可能持续失败需人工；正确是重试 + 告警 + 挂账。
- ❌ "强一致最安全必须上" —— 错在不顾 RT 与吞吐；正确是看业务容忍度选最终一致。

---

#### 14.6 【退款】部分退款、重复退款、退款金额大于实付
> **难度**：L2 进阶 ｜ **考察频次**：中 ｜ **目标能力**：模型设计 ｜ **标签**：退款、部分退款、防超额

**业务场景与约束**
- **业务背景**：用户下单后可申请退款，同一订单支持多次部分退款；退款走原渠道退回用户；退款算错（多退、重复退）是直接资损且难追回。
- **量化约束**：退款率 8%；同一订单支持多次部分退款（最多 5 次）；退款走原渠道 T+1 到账；单笔退款金额离散（几元到上万元）。

**渐进式追问**
1. 为什么不能用订单上一个 `refund_amount` 字段就够？
2. 你设计怎样的退款单模型来支撑多次部分退款？
3. 并发两次退款请求，怎么防止累计退款超过实付？
4. 怎么防止同一笔退款被重复发起（重复退款单）？
5. "退款就是把余额加回去"这个理解为什么是错的？
6. 渠道退款成功但本地状态还是失败（或反之），对账怎么发现？

**参考答案要点**
- 模型：必须有独立 `refund_order` 表，每笔退款一张单，带 `refund_no`（全局唯一）、`order_id`、`refund_amount`、`status`、关联原支付单。订单上只存 `total_refunded`（累计已退）作冗余展示，**不能**只用这一个字段决策，因为多次部分退要靠明细追溯、要对账、要防重复。
- 防超额：退款时走状态机 + 乐观锁，`UPDATE refund_order ... WHERE id=? AND status='INIT' AND (total_refunded + ?) <= paid_amount`，数据库原子校验累计不超实付；应用层同时用 `total_refunded` 行锁保护。
- 防重复：`refund_no` 或 `(order_id, out_refund_no)` 加唯一索引，渠道回调用 `out_refund_no` 幂等，重复退款单插入即冲突拒绝。
- 为什么不是"加余额"：退款是退回原支付渠道（微信/银行卡），不是平台账户加钱；即使账户余额支付，也走渠道回退流程并有独立退款流水，否则和对账、税务、渠道账单全对不上。
- 渠道对账：以 `refund_no` 关联本地与渠道退款状态，发现"渠道成功本地失败"补状态、"本地成功渠道失败"触发重试/挂账。

**可接受的关键结论**
- 退款要独立 `refund_order` 模型，不能只靠订单上一个 `refund_amount`。
- 防超额靠 `total_refunded + ? <= paid_amount` 的原子校验 + 乐观锁。
- 防重复靠 `refund_no` / `(order_id, out_refund_no)` 唯一索引幂等。
- 退款退回原渠道，不是"余额加回去"。
- 退款与渠道以 `refund_no` 对账，发现状态不一致即补/重试。

**考察意图**
考模型设计中对"部分退款 + 防超额"的严谨度。L1 会说"订单加个退款金额字段"；L2 分水岭是能独立出 `refund_order`、用唯一索引防重复、用累计金额原子校验防超额，并纠正"退款=加余额"的误区。

**常见错误回答**
- ❌ "订单加个 `refund_amount` 字段记录退款" —— 错在多次部分退无法追溯且易超额；正确是独立 `refund_order`。
- ❌ "退款就是把用户余额加回去" —— 错在绕过原渠道、对账税务全乱；正确是走原渠道回退。
- ❌ "并发退款先查累计再减" —— 错在查询与扣减间有窗口会超额；正确是 `WHERE (total_refunded+?) <= paid` 原子判。
- ❌ "同一笔退款重试就再发一次" —— 错在重复退款资损；正确是 `out_refund_no` 唯一幂等。
- ❌ "退款状态以本地为准忽略渠道" —— 错在渠道成功本地失败会漏；正确是对账补状态。

---

#### 14.7 【消息可靠】本地消息表 vs MQ 事务消息怎么选
> **难度**：L2 进阶 ｜ **考察频次**：中 ｜ **目标能力**：方案选型 ｜ **标签**：消息可靠、本地消息表、事务消息

**业务场景与约束**
- **业务背景**：业务操作（如扣库存）和发消息（通知下游）要一致，不能"扣了库存下游没收到"或"消息发了但本地没扣"；消息丢了会导致后续流程断裂。
- **量化约束**：`Kafka 4.0` 集群为唯一 MQ（仅 `KRaft`，无 `ZooKeeper`）；业务库 `MySQL`；允许自建补偿任务；消息端到端不丢要求 99.99%。

**渐进式追问**
1. `Kafka 4.0` 的 EOS（精确一次）能跨系统保证"本地事务 + 发消息"一致吗？
2. 既然 `Kafka` 没有原生事务消息，你怎么用本地消息表兜底？
3. 本地消息表怎么保证"本地事务成功则消息必投递、失败则必不投递"？
4. 消息消费端怎么保证幂等，重复投不会重复处理？
5. 投递失败进死信后，怎么补偿？
6. 和 `RocketMQ` 事务消息（半消息 + 回查）比，两种方案你选哪个、为什么？

**参考答案要点**
- `Kafka` EOS 边界：`Kafka 4.0` 的 EOS（`enable.idempotence` + 事务）只在 `Kafka` 内部流转（生产→消费→再生产）保证精确一次，**跨系统**——即"`MySQL` 本地事务提交"与"`Kafka` 发消息"这两个系统之间——`Kafka` 管不了，仍需业务侧幂等或本地消息表。
- 本地消息表：`MySQL` 内同事务写业务数据和 `msg_outbox`（`status=PENDING`）；事务提交即消息落库。独立补偿任务轮询 `PENDING` 消息发 `Kafka`，成功置 `SENT`；失败重试，超次进死信。这样"本地成功→消息必发"由同事务保证，"发失败→补偿重发"由轮询保证。
- 消费幂等：消费端用 `(topic, partition, offset)` 或业务 `biz_key` 去重表/状态机，重复投因唯一键冲突跳过；`Kafka` 本身 `at-least-once`，幂等在消费侧兜底。
- `RocketMQ` 事务消息：半消息先发（broker 不可见），本地事务成功 `Commit`、失败 `Rollback`；broker 未确认会回查本地事务状态（回查次数有限，要注意悬挂：回查前本地已回滚但半消息被消费）。
- 选型：已锁定 `Kafka 4.0` 且无原生事务消息 → 选本地消息表，侵入性低、一致性靠同事务、运维只需管补偿任务；若用 `RocketMQ` 则事务消息更省心但引入回查与悬挂复杂度。

| 维度 | 本地消息表 | RocketMQ 事务消息 |
|---|---|---|
| 侵入性 | 低（加表+补偿） | 中（半消息+回查） |
| 一致性强度 | 最终一致 | 最终一致 |
| 运维成本 | 自管补偿任务 | broker 托管 |
| 适用 | 已用 Kafka | 用 RocketMQ |

**可接受的关键结论**
- `Kafka` EOS 只保 `Kafka` 内部，跨系统一致仍需本地消息表或幂等。
- 本地消息表靠"业务+消息同事务"保证必发/必不发。
- 消费端必须幂等，`Kafka` 是 `at-least-once`。
- `RocketMQ` 事务消息有回查次数与悬挂坑。
- 已锁定 `Kafka 4.0` 就选本地消息表，侵入低、自管补偿。

**考察意图**
考方案选型里"用什么、为什么不用另一个"。L2 分水岭是能点出 `Kafka` EOS 的跨系统盲区，从而选择本地消息表，并说清回查/悬挂这类框架坑；而非泛泛说"消息会丢要重试"。

**常见错误回答**
- ❌ "开 `Kafka` EOS 就保证本地和消息一致" —— 错在 EOS 不跨 `MySQL`/`Kafka` 两系统；正确是本地消息表兜底。
- ❌ "发消息失败 `catch` 不管" —— 错在本地成功下游没收到；正确是同事务落消息表 + 补偿。
- ❌ "消费重复投无所谓" —— 错在重复处理资损/脏数据；正确是消费幂等。
- ❌ "RocketMQ 回查无限次" —— 错在回查次数有限会悬挂；正确是限定回查 + 状态兜底。
- ❌ "半消息发出去就能被消费" —— 错在半消息 `Commit` 前不可见；正确是 `Commit` 后才投递。
- ❌ "两种方案一样随便选" —— 错在忽略 MQ 已锁定的约束；正确是依现有 `Kafka 4.0` 选型。

---

#### 14.8 【金额计算】为什么 0.1 + 0.2 ≠ 0.3 会引发对账差异
> **难度**：L1 基础 ｜ **考察频次**：中 ｜ **目标能力**：工程细节 ｜ **标签**：金额计算、BigDecimal、精度

**业务场景与约束**
- **业务背景**：系统处理多币种交易与费率计算，任何一分钱误差都会在对账时放大成差异，财务要求零差异；新人最容易在这里埋坑。
- **量化约束**：支持 8 种币种；费率保留 3 位小数；日均 80 万单；对账要求零差异；最小货币单位到分（或更小币种到厘）。

**渐进式追问**
1. 为什么 `0.1 + 0.2` 在程序里不等于 `0.3`，会引发什么后果？
2. 金额在数据库和代码里应该存成什么类型？
3. 用 `BigDecimal` 时为什么必须传 `String` 而不是 `double` 构造？
4. 舍入模式全局怎么统一，为什么不能有的地方四舍五入有的地方 bankers？
5. 汇率和费率计算时，"先乘后舍"还是"先舍后乘"结果一样吗？
6. 金额在 `JSON` 里序列化为什么要用字符串而不是数值？

**参考答案要点**
- 根因：`double`/`float` 是二进制浮点，0.1、0.2 无法精确表示，相加得 `0.30000000000000004`，直接比较或累加会在 80 万单里放大成可见差异，对账必炸。
- 存储：数据库用 `BIGINT` 存最小货币单位（分/厘），彻底避开小数；代码用 `BigDecimal` 做运算。`DECIMAL` 只是存储精度，`Java` 侧取出仍是 `BigDecimal`，但绝不让 `double` 参与运算。
- 构造：必须用 `new BigDecimal("0.1")`，`String` 构造能精确表示；`new BigDecimal(0.1)` 会把 `double` 的误差带进来，一开始就错了。
- 舍入：全局规范写死一个模式（如 `ROUND_HALF_UP` 或银行家舍入 `ROUND_HALF_EVEN`），所有金额运算统一调用，禁止混用，否则同一笔账算两遍结果不同。
- 顺序：先乘后舍 vs 先舍后乘结果不同（如费率 0.333 × 3 元，先乘得 0.999 再舍 vs 先舍 0.33 再乘得 0.99），必须规范计算顺序并在单测覆盖边界值。
- 传输：`JSON` 金额用 `String` 而非数值，避免前端/中间层 `double` 解析再次丢精度。

**可接受的关键结论**
- 金额用 `BIGINT` 存最小单位，`BigDecimal` 运算，绝不用 `double`。
- `BigDecimal` 必须 `String` 构造，不能传 `double`。
- 舍入模式全局唯一，不能混用。
- 计算顺序（先乘后舍/先舍后乘）要写进规范，影响结果。
- `JSON` 金额用 `String` 序列化防精度丢失。

**考察意图**
这是本模块唯一的 L1 锚点，验证应届生是否具备基础工程严谨度：能不能说出"`double` 不精确 → 用 `BigDecimal("0.1")` → 舍入模式统一"。答出浮点误差根因 + 正确存储类型即及格；能补上"计算顺序"和"`JSON` 用 String"是加分。

**常见错误回答**
- ❌ "金额用 `double` 存就行，差一点没关系" —— 错在 80 万单放大成对账差异；正确是 `BIGINT` 分/厘。
- ❌ "`new BigDecimal(0.1)` 没问题" —— 错在把 `double` 误差带入；正确是 `new BigDecimal("0.1")`。
- ❌ "舍入哪方便用哪" —— 错在同笔账算两遍不一致；正确是全局唯一模式。
- ❌ "先舍后乘更省事" —— 错在结果与先乘后舍不同造成差异；正确是规范顺序。
- ❌ "JSON 金额传数值类型" —— 错在解析再丢精度；正确是 `String`。
- ❌ "0.1+0.2 不等于 0.3 是 bug 不管" —— 错在这是浮点本质不是 bug；正确是换 `BigDecimal`。

## 第 15 章 数据同步与一致性场景

> 本章聚焦"数据搬来搬去"。核心不是把数据写过去，而是**怎么证明它是对的、错了怎么补、要跑路怎么回滚**。

#### 15.1 【缓存一致性】更新 DB 之后删缓存还是更新缓存？并发下还有没有坑？
> **难度**：L2 进阶 ｜ **考察频次**：高 ｜ **目标能力**：边界分析 ｜ **标签**：缓存一致性、Cache Aside

**业务场景与约束**
- **业务背景**：商品详情页是电商核心读链路，日均被访问数亿次；前端和 App 都依赖缓存降低 DB 压力；一旦返回长期脏数据，用户会看到错误价格与下架商品，引发客诉与资损。
- **量化约束**：商品详情读 `8 万 QPS`、写 `200 TPS`、允许 `1 秒`内短暂不一致、不允许出现长期脏数据（脏数据存活超过 1 秒即算缺陷）。

**渐进式追问**
1. 更新数据时，应该先动 DB 还是先动缓存，为什么？
2. 为什么标准做法选"更新 DB 后删缓存"而不是"更新缓存"？
3. 并发下"读未命中 → 读到旧 DB 值 → 才回写缓存"这个脏回写窗口，到底在什么条件下才会真正发生？
4. 这个脏窗口发生的概率有多大，能不能用数字说明它几乎可忽略？
5. 删除缓存这一步如果失败（Redis 抖动/超时），怎么补？
6. 延迟双删能不能彻底解决这个窗口，它又引入了什么问题？
7. 最后能不能加一层兜底，让任何残留脏数据都能自愈？

**参考答案要点**
采用 Cache Aside（旁路缓存）标准顺序：**先更新 DB，再删除缓存**。原因：更新缓存需重新计算（可能要查多张表聚合），成本高且并发写时容易"后到的旧值覆盖先到的新值"；而删缓存是 `O(1)` 幂等操作，下次读自然回填最新值。

```java
// Cache Aside：先更 DB，再删缓存（删除失败进 MQ 重试 / 订阅 binlog 补偿）
orderMapper.update(order);
try { redis.del("order:" + id); }
catch (Exception e) { mq.publish("cache:del", "order:" + id); }
```

脏回写窗口确实存在：线程 A 读未命中 → 读 DB 拿旧值 → 正要写回时，线程 B 完成"更新 DB + 删缓存"，A 才把旧值写回，造成脏数据。但**触发条件极苛刻**：必须 A 的读恰好未命中（高命中率下概率极低），且 A 读 DB 与写缓存之间，B 的整个"更新 + 删缓存"必须紧凑插入。按 `8 万 QPS`、`200 TPS`，读未命中率约 `0.5%`，单次读内发生交错的概率约 `200/80000 × 交错占比`，量级在十万分之一以下，且脏数据最多存活 1 秒。删缓存失败用**重试兜底**：本地记失败日志走 MQ 重试，或订阅 `binlog`（Canal）做删除补偿。延迟双删（更新后删一次、延迟几百毫秒再删）能压低窗口但**不能根除**，还增加无效删除与延迟，仅适合强一致诉求。最后用**缓存 TTL 兜底**（如 `300 秒`），任何残留脏数据都会到期自愈。

**可接受的关键结论**
- 标准顺序固定为"先更新 DB，再删除缓存"，原因不是玄学而是成本与幂等。
- 脏回写窗口只在"读未命中 + 并发写紧凑插入"时触发，量级极低，可量化说明。
- 删除失败必须补偿（MQ 重试 / 订阅 binlog），否则缓存永脏。
- 延迟双删不根除问题且引入额外延迟，仅在强一致诉求下使用。
- 缓存 TTL 是最后兜底，保证任何脏数据都能自愈。

**考察意图**
考察边界分析能力：是否停留在"先删还是后删"的口号，还是能说清脏窗口的**触发条件与概率量级**。区分 L2 与 L3 的分水岭在于：能否用数字说明该窗口"几乎可忽略 + TTL 自愈"，而非空谈"可能不一致"。

**常见错误回答**
- ❌ "先删缓存再更新 DB，这样最安全" —— 错在删除后、DB 提交前若有读请求，会回填旧值且脏数据直到 TTL 才消失；正确做法是先更 DB 后删缓存。
- ❌ "更新缓存比删缓存更实时" —— 错在忽略计算成本与并发写覆盖，且删缓存天然幂等；正确做法是删缓存靠回源回填。
- ❌ "并发下一定会出现脏数据" —— 错在没说明窗口触发条件与概率；正确做法是给出发生条件与量级并用 TTL 兜底。
- ❌ "用分布式锁包住读写保证强一致" —— 错在牺牲读性能（8 万 QPS 会阻塞），正确做法是接受秒级最终一致 + 兜底而非强一致。
- ❌ "删缓存失败无所谓，下次 TTL 会清" —— 错在 TTL 期间可能多次读到脏值；正确做法是失败即补偿删除。

---

#### 15.2 【缓存异常】大促零点缓存集体失效 + 一个热点 key 被打爆
> **难度**：L2 进阶 ｜ **考察频次**：高 ｜ **目标能力**：组合防御 ｜ **标签**：缓存穿透、击穿、雪崩、热点 key

**业务场景与约束**
- **业务背景**：大促零点大量商品缓存集中过期或集群刚重启，瞬时流量洪峰打向 DB；同时某爆款单品成为超级热点，单 key 流量远超单分片承载，处理不当会连锁拖垮整条读链路。
- **量化约束**：预热 key `300 万`个、热点单 key `20 万 QPS`、Redis Cluster `32 分片`、不允许 DB 被打穿（DB 最大承接应 `5000 QPS`）。

**渐进式追问**
1. 大量 key 同时失效导致请求穿透到 DB，这属于穿透、击穿还是雪崩，三者区别是什么？
2. 针对"穿透"（查不存在的数据），怎么防？
3. 针对"击穿"（单热点 key 失效瞬间海量请求涌向 DB），互斥锁重建、逻辑过期、永不过期三种方案代价分别是什么？
4. 针对"雪崩"（大量 key 同时过期），怎么从 TTL 设计上避免？
5. 单 key 20 万 QPS 远超单分片上限，怎么识别并打散热点 key？
6. 本地缓存（Caffeine）在这一层能挡掉多少流量，又会带来什么一致性问题？
7. 多级缓存 + 熔断降级组合后，如何验证 DB 不会被打穿？

**参考答案要点**
先分清三类问题。穿透 = 查不存在的数据，用**布隆过滤器**拦截非法 key，或对空结果缓存短 `TTL`（如 `60 秒`）防重复穿透；注意空值缓存只对"确实不存在"的 key 生效，避免污染。击穿 = 单热点 key 失效瞬间，三种打法：`互斥锁重建`（`SETNX` 抢锁，仅一个线程回源，代价是锁等待与死锁风险）、`逻辑过期`（值里带过期时间、异步重建，代价是短暂返回旧值）、`永不过期 + 异步刷新`（后台定时刷新，代价是内存常驻与更新不及时）。雪崩 = 大量 key 同过期，核心是**TTL 加随机 jitter**（如基础 `300s ± 60s`）打散失效点，配合多级缓存与熔断（DB 承压超阈值即降级返回兜底）。

```java
// 互斥锁重建：仅一个线程回源，其余等待/降级
if (!redis.setnx("lock:" + key, "1", 3, SECONDS)) {
    return getFromLocalCache(key); // 降级取本地旧值
}
try { String val = loadFromDb(key); redis.setex(key, 300, val); }
finally { redis.del("lock:" + key); }
```

热点 key 用 `OBJECT FREQ`（Redis 7.0 起 LFU，替代旧 ziplist 统计）或代理层统计识别，打散为 `key_0..key_31` 分散到 32 个分片，单分片只承接约 `6000 QPS`。再在应用层加 **Caffeine 本地缓存**挡掉 `80%` 以上读（20 万 QPS 落到 Redis 仅约 4 万），代价是本地多实例间短暂不一致，靠短 `TTL`（如 `2 秒`）收敛。最后用熔断（如 Sentinel）在 DB QPS 逼近上限时降级，确保 DB 不被打穿。代价对比：布隆过滤器省内存但有误判；互斥锁保一致但有等待；本地缓存削峰但引入不一致，需按业务取舍。

**可接受的关键结论**
- 穿透、击穿、雪崩是三件事，防御手段不同，不能混为一谈。
- 雪崩靠 TTL 随机 jitter 打散，而非统一过期。
- 热点 key 必须识别后打散到多分片，单实例纵向扩容无效。
- 本地缓存（Caffeine）能削峰但引入多实例不一致，需短 TTL 收敛。
- 熔断降级是最后护栏，确保 DB 不被打穿。

**考察意图**
考察组合防御与权衡：能否把穿透/击穿/雪崩区分清楚并分别选型，而非混用一套方案。分水岭在于能否说出热点 key 必须**打散到多分片**（单分片有上限），以及本地缓存的代价是一致性。

**常见错误回答**
- ❌ "给所有 key 设一样长的 TTL 最省事" —— 错在制造集体失效引发雪崩；正确做法是 TTL 加随机 jitter 打散。
- ❌ "热点 key 直接加更大机器规格顶住" —— 错在单 key 受限于单分片/单线程，纵向扩容无效；正确做法是 key 打散到多分片。
- ❌ "穿透用缓存 null 就行，不用布隆过滤器" —— 错在恶意遍历不存在的 key 仍会击穿（null 也占内存）；正确做法是布隆过滤器前置拦截。
- ❌ "本地缓存能彻底解决热点" —— 错在忽略多实例不一致与内存放大；正确做法是用短 TTL 收敛并配合 Redis 双层。
- ❌ "互斥锁重建永远不会出问题" —— 错在锁未设超时会导致死锁、线程堆积；正确做法是锁带超时 + 看门狗续期。

---

#### 15.3 【binlog 同步】Canal / Flink CDC 同步到 ES 和数仓
> **难度**：L3 高级 ｜ **考察频次**：中 ｜ **目标能力**：数据管道设计 ｜ **标签**：CDC、Flink、幂等、乱序

**业务场景与约束**
- **业务背景**：主库订单/商品变更需近实时同步到 ES（搜索）和数仓（分析）；下游消费方依赖准确数据做搜索排序与报表，数据错乱会直接导致搜索结果错误与统计失真。
- **量化约束**：主库 `5000 TPS` 写入、同步链路 RT 秒级、ES 索引 `2 亿`文档、允许分钟级最终一致。

**渐进式追问**
1. CDC 链路默认是"至少一次"还是"恰好一次"，为什么不能假设不丢不重？
2. 下游 ES/数仓怎么保证重复消息不写脏，靠去重表还是别的方式？
3. 消息乱序（后发生的变更先到）怎么处理，Flink 里用什么机制？
4. 全量初始化（存量 2 亿）和增量（binlog）怎么平滑切换，切换点会不会丢或重？
5. 上游 DDL（加列/改类型）同步链路怎么兼容与告警？
6. 怎么监控同步延迟，lag 指标怎么取？
7. 怎么证明 ES/数仓和主库最终是一致的，校验手段是什么？

**参考答案要点**
基于 MySQL 8.0（InnoDB、RR 隔离级别、`row` 格式 binlog），CDC 本质是**至少一次**语义（Kafka 4.0 起仅支持 KRaft，producer 重试、consumer 重平衡都可能重投），所以下游必须幂等。正确做法不是建"去重表"，而是**幂等 upsert**：以业务主键（如 `order_id`）做覆盖写，配合**版本号/时间戳比对**（写入时仅当新值更新才覆盖），旧版本迟到直接丢弃。

```sql
-- 幂等 upsert：以业务主键覆盖写（版本比对在应用层判断）
INSERT INTO dw_order(order_id, status, update_time)
VALUES(#{id}, #{status}, #{ts})
ON DUPLICATE KEY UPDATE
  status = VALUES(status), update_time = VALUES(update_time);
```

乱序用 Flink `upsert-kafka` + 按主键分区，保证同主键进同一并行度；用**水位线（watermark）+ 状态 TTL** 做"按主键取最新"：到达的变更若 `update_time` 小于已存值则忽略。全量/增量切换靠**存量快照 + 增量位点**：先记录 binlog 位点 `P`，再导存量（此时新写入已进 binlog 不会丢），导完用 `P` 起增量；重叠窗口用幂等 upsert 消重，保证不丢不重。DDL 变更需链路支持 schema 演进（如 Avro + registry）并告警人工确认，避免字段错位。监控用 Kafka consumer `lag` + Flink `currentWatermark` 反映延迟。校验用 `count`/`sum` 比对 + 抽样 `detail` 比对，定位差异批次。

**可接受的关键结论**
- CDC 默认至少一次，下游必须幂等，靠业务主键 upsert + 版本比对。
- 乱序用按主键分区 + 时间戳比对取最新，不依赖到达顺序。
- 全量/增量切换先记位点再导存量，重叠区靠幂等消重保证不丢不重。
- DDL 需 schema 演进与告警，不能静默兼容。
- 一致性靠 count/sum + 抽样 detail 校验，差异要可定位到批次。

**考察意图**
考察数据管道设计：能否把"至少一次 + 幂等 + 乱序"作为默认前提，而非假设链路可靠。分水岭是能否说清全量/增量切换的位点机制，以及用幂等 upsert 而非去重表。

**常见错误回答**
- ❌ "CDC 是恰好一次，不用管重复" —— 错在 Kafka/Canal 默认至少一次，重投必然发生；正确做法是下游幂等 upsert。
- ❌ "用一张去重表记 messageId 防重" —— 错在去重表自身有 TTL/容量问题且无法处理乱序覆盖；正确做法是用主键做幂等覆盖写。
- ❌ "乱序就按到达顺序写" —— 错在旧值会覆盖新值；正确做法是按 `update_time`/`版本号` 比对取最新。
- ❌ "全量导完直接切增量，中间空窗会丢" —— 错在没先记位点再导存量；正确做法是先位点后存量，重叠区靠幂等消重。
- ❌ "DDL 变更同步链路自动兼容不用担心" —— 错在字段类型/顺序变化会错位甚至写崩；正确做法是 schema 演进 + 告警。

---

#### 15.4 【不停机迁移】单表 8 亿行拆成 64 张分表
> **难度**：L3 高级 ｜ **考察频次**：中 ｜ **目标能力**：迁移方案 ｜ **标签**：分表、双写、灰度切流、回滚

**业务场景与约束**
- **业务背景**：单表 8 亿行已成性能瓶颈，慢查询频发拖垮整体 RT；需水平拆成 64 张分表，但迁移期间业务不能停，且一旦出问题必须能秒级回滚，否则就是线上事故。
- **量化约束**：`8 亿`行、日均写入 `2000 万`、允许秒级只读窗口、必须可回滚、不能丢数据。

**渐进式追问**
1. 迁移核心思路是双写还是全量拷贝后切换，为什么选双写？
2. 双写时新旧表都写，如果写新表失败怎么办，怎么降级？
3. 8 亿存量怎么搬，怎么限速避免打爆主库，断了怎么续？
4. 怎么证明新表和旧表数据一致？
5. 切流怎么灰度，从哪类流量开始？
6. 出问题怎么回滚，回滚是切回去还是反向双写？
7. 分片键怎么选，选错会有什么后果？

**参考答案要点**
核心是**双写 + 灰度切读**：通过开关同时写新旧表，校验一致后逐步把读切到新表。双写失败不能影响主流程——写新表失败应**降级为记补偿日志/MQ**，旧表仍正常，保证不丢；等新表恢复后由补偿任务补齐。存量迁移按**主键分批**（如每批 `1 万`行）限速（如每秒 `500` 批）迁移，避免压垮主库；用游标推进，断点续跑只需记录 `last_id`。

```sql
-- 分批迁移游标，断点续跑靠 last_id
INSERT INTO order_new_${shard}(id, user_id, amount, update_time)
SELECT id, user_id, amount, update_time FROM order_old
WHERE id > #{lastId} ORDER BY id LIMIT 10000;
```

校验做**全量 checksum**（按主键区间分片，`count` + `sum(关键数值列)` + `max(update_time)` 比对）+ 抽样逐行比对 + 实时增量比对（双写期间新旧差异应趋近于 0）。切流灰度：先放**白名单用户**读新表 → 观察无异常 → 放 `1%` 流量 → 全量切读新表 → 最后停写旧表。回滚用**反向双写**（新表变更同步回旧表）+ 切回读旧表，因为旧表数据一直被双写保持最新，所以回滚是秒级的。分片键必须**贴合查询模式**（如按 `user_id` 分片，则按用户查都落单表）；选错（如按无意义 `id` 但业务都按 `order_no` 查）会导致跨 64 片广播查询，比单表还慢。分布式 ID 用雪花或号段，避免自增冲突与热点。

**可接受的关键结论**
- 迁移用双写 + 灰度切读，开关控制，绝不一次性切换。
- 双写失败降级为补偿日志，保证主流程不丢数据。
- 存量迁移按主键分批限速 + 断点续跑，避免打爆主库。
- 校验靠全量 checksum + 抽样 + 实时增量，不能只看 count。
- 回滚靠反向双写 + 切读旧表，因为旧表一直被保持最新。

**考察意图**
考察迁移方案的工程稳健性：能否把"可回滚""不丢数据"作为一等约束。分水岭是能否讲清双写失败降级与反向双写回滚，而非只想"切过去"。

**常见错误回答**
- ❌ "直接 rename 新表上线最快" —— 错在一旦新表有问题无法回滚且停写期间丢数据；正确做法是双写 + 灰度切流。
- ❌ "存量一次性全量迁移" —— 错在打爆主库引发线上故障；正确做法是主键分批 + 限速 + 断点续跑。
- ❌ "校验只比对行数 count" —— 错在一多一少互相抵消；正确做法是 checksum + 抽样 detail 比对。
- ❌ "分片键随便选一个均匀的字段" —— 错在忽略查询模式导致跨片广播；正确做法是贴合高频查询维度。
- ❌ "停写旧表后再发现数据不对就完了" —— 错在没保留反向双写兜底；正确做法是回滚靠反向双写 + 切读。

---

#### 15.5 【缓存预热与热点发现】大促前 30 分钟怎么预热？
> **难度**：L1 基础 ｜ **考察频次**：中 ｜ **目标能力**：工程落地 ｜ **标签**：缓存预热、热点探测、本地缓存

**业务场景与约束**
- **业务背景**：大促前若不预热，零点流量会瞬间击穿缓存打向 DB；运营也会圈定一批必抢商品。这是一道偏工程落地的题，重点是把"别把 DB 打爆"这个常识变成可执行的步骤。
- **量化约束**：`300 万`商品、预热窗口 `30 分钟`、Redis 内存 `64G`、预热过程本身不能打爆 DB。

**渐进式追问**
1. 怎么知道哪些商品需要预热，数据是哪来的？
2. 预热时一次性把所有 key 灌进 Redis 会不会有问题？
3. 怎么给预热限速，避免把 DB 打爆？
4. 大促进行中怎么发现"正在变热"的新热点？
5. 本地缓存（Caffeine）在这一层起什么作用？
6. 预热失败或 Redis 内存不够怎么办，怎么兜底？

**参考答案要点**
预热名单来自三个来源：**离线挖掘**（历史 PV 高的商品 + 加购/收藏 TopN）、**运营清单**（大促主推坑位）、实时兜底。拿到名单后**不能一把全灌**，否则瞬时高并发回源会打爆 DB。正确做法是**限速预热**：用令牌桶（如每实例每秒 `500` 个 key）分批从 DB 读取并写入 Redis，`30 分钟`窗口足够平滑铺完 300 万 key（500 × 1800s = 90 万/实例，多实例并行即可）。

```java
// 令牌桶限速预热，避免打爆 DB
RateLimiter limiter = RateLimiter.create(500); // 500 key/s/实例
for (String key : hotKeys) {
    limiter.acquire();
    redis.setex(key, ttl, loadFromDb(key));
}
```

大促中热点靠**实时探测**：客户端/网关上报访问计数，或利用 Redis 7.0 的 LFU（`OBJECT FREQ`）识别高频 key，命中后自动加载到**本地缓存 Caffeine** 挡一层，减少 Redis 网络往返。内存方面，Redis `maxmemory-policy` 选 `allkeys-lru`（允许所有 key 被淘汰，内存满时自动回收冷 key），避免 `volatile-lru` 因未设 TTL 的 key 无法淘汰而 OOM。要注意 big key（如超长 value）会阻塞单线程，预热时拆分。兜底：**熔断 + 限流降级到 DB**，但 DB 侧必须有限流（如信号量），防止预热/突发把 DB 打穿；预热失败不影响线上已缓存数据。

**可接受的关键结论**
- 预热名单 = 离线挖掘 + 运营清单 + 实时兜底，三源合流。
- 预热必须限速（令牌桶 + 分批），绝不能一把灌爆 DB。
- 实时热点靠客户端上报或 Redis LFU（`OBJECT FREQ`）探测。
- 内存淘汰选 `allkeys-lru`，避免冷 key 无法回收导致 OOM。
- 兜底用熔断 + DB 侧限流，预热失败不影响已缓存数据。

**考察意图**
考察工程落地基本功：能否把"预热不能打爆 DB"作为铁律，并用限速/探测/淘汰策略/兜底四件套落地。这是本章唯一的 L1 锚点，分水岭是能否讲清限速与 `allkeys-lru` 的选择理由。

**常见错误回答**
- ❌ "大促前把所有商品一次性全量预热" —— 错在瞬时回源打爆 DB；正确做法是令牌桶限速分批预热。
- ❌ "热点只靠预热名单，不实时探测" —— 错在漏掉临时爆款；正确做法是离线名单 + 实时 LFU 探测双轨。
- ❌ "Redis 内存满了就加机器" —— 错在没设淘汰策略导致 OOM；正确做法是配 `allkeys-lru` 自动回收冷 key。
- ❌ "本地缓存 Caffeine 不需要过期" —— 错在多实例间数据变更无法感知，长期不一致；正确做法是短 TTL 定期刷新。
- ❌ "预热失败就重试不限速" —— 错在重试风暴二次打爆 DB；正确做法是限流 + 熔断降级。

---

#### 15.6 【一致性校验】怎么证明两个异构数据源是一致的？
> **难度**：L3 高级 ｜ **考察频次**：中 ｜ **目标能力**：验证设计 ｜ **标签**：数据校验、checksum、对账

**业务场景与约束**
- **业务背景**：MySQL 主库 8 亿行与数仓需定期核对一致；资损排查与合规审计都要求"能证明没丢没重"，且差异要能定位到具体行，否则对账报告无人敢签字。
- **量化约束**：MySQL `8 亿`行 vs 数仓、不能全表锁、每天跑一次、差异必须可定位到具体行。

**渐进式追问**
1. 只比对两边的 count 总行数够不够证明一致？
2. 海量数据怎么分批做校验而不锁全表？
3. 为什么会有"假差异"，怎么排除？
4. checksum 只定位到批次还不够，怎么进一步定位到具体行？
5. 偶发抖动导致的差异怎么避免误报？
6. 校验任务本身不能影响线上，怎么隔离资源？
7. 校验结果给人看要长什么样？

**参考答案要点**
只比对 `count` **远远不够**：少一行又多一行会互相抵消，两边 count 相等但数据已错。正确做法是**分批 checksum**：按主键区间或时间分片（如每 `100 万`行一批），每批比对 `count`、`sum(关键数值列)`、`max(update_time)`。这样差异能**定位到具体批次**而非全表。全程走**覆盖索引/分批 `WHERE id BETWEEN ? AND ?`**，不锁全表，对线上无感。

```sql
-- 分批 checksum：每批比对 行数/数值和/最大更新时间
SELECT COUNT(*), SUM(amount), MAX(update_time)
FROM orders
WHERE id BETWEEN #{start} AND #{end}
  AND update_time < NOW() - INTERVAL 5 MINUTE;
```

"假差异"来自**同步延迟窗口**：校验瞬间主库有在途未同步到数仓的变更，会造成批次不一致。必须设**延迟窗口**（如仅比对 `update_time < 当前时间 - 5 分钟` 的冷数据），热数据留给下次。定位到批次后做**抽样 detail 比对**（逐行 `SELECT` 关键字段），精确定位到具体行。为避免偶发抖动误报，差异要做**二次复检**（隔几分钟重跑该批次），确认仍不一致才落表。差异落`差异表`并触发**自动补偿**（重跑该批同步）。资源隔离：校验任务走**只读从库 / 低优先级队列**，限流扫描，监控其对主库 QPS 的影响。对账报告要给人看：含批次、差异行数、样例主键、成因假设，便于人工确认。

**可接受的关键结论**
- count 相等不可信，必须分批 checksum（count+sum+max(update_time)）。
- 分批走覆盖索引不锁全表，对线上无感。
- 必须排除同步延迟窗口的假差异，否则全是误报。
- 差异经二次复检后落表并自动补偿，形成闭环。
- 校验任务需资源隔离 + 限流，对账报告要能给人看。

**考察意图**
考察验证设计思维：能否把"证明一致"当作可量化、可定位、可闭环的工程问题。分水岭是能否指出 `count` 抵消陷阱与延迟窗口假差异，而非只说"跑个脚本比对"。

**常见错误回答**
- ❌ "count 相等就说明一致" —— 错在一多一少抵消；正确做法是分批 checksum + 抽样 detail。
- ❌ "全表锁住再比对最准" —— 错在 8 亿行锁表会拖垮线上；正确做法是分批覆盖索引扫描不锁全表。
- ❌ "出现差异立刻告警人工处理" —— 错在没排除延迟窗口假差异与偶发抖动；正确做法是延迟窗口 + 二次复检。
- ❌ "校验在凌晨随便跑无所谓" —— 错在仍可能打爆从库/主库；正确做法是资源隔离 + 限流。
- ❌ "差异只记日志不补偿" —— 错在只发现问题不闭环；正确做法是差异落表 + 自动补偿。

---

#### 15.7 【幂等方案】Token 预领取、唯一索引、状态机，边界在哪？
> **难度**：L2 进阶 ｜ **考察频次**：高 ｜ **目标能力**：方案边界 ｜ **标签**：幂等、Token、唯一索引、状态机

**业务场景与约束**
- **业务背景**：支付/下单类写接口会被网关重试、MQ 重投，重复请求若不加防护会造成重复扣款/重复下单。需清楚三种幂等手段各自的边界，避免只知其一。
- **量化约束**：写接口 `3000 TPS`、需支持网关重试与 MQ 重投、存储 MySQL + Redis。

**渐进式追问**
1. 幂等靠"前端拦住重复点击"够吗，为什么不够？
2. Token 预领取方案怎么实现，天然有什么代价？
3. 删除已用 token 时，为什么必须 `DEL` 原子操作而不是先查后删？
4. 唯一索引方案为什么最可靠，怎么正确识别冲突？
5. 状态机 CAS 适合什么场景，怎么判断成功？
6. 三者组合时推荐姿势是什么？
7. 幂等键怎么设计，幂等和并发安全是一回事吗？

**参考答案要点**
幂等不能只靠前端——网关重试、MQ 重投都在网络层，前端拦不住。三种手段边界不同：**Token 预领取**：业务前先向 Redis 申请 `token`（如 `SET token uuid EX 300`），提交时携带，服务端 `DEL` 成功才处理；代价是多一次 RT，且 token 需设过期防堆积。**关键坑**：删除必须用原子 `DEL`，若"先 `GET` 判断存在 → 再 `DEL`"在并发下两个请求都可能通过，失去防重意义。

```sql
-- 状态机 CAS：看影响行数判断是否推进
UPDATE `order` SET status = 'PAID'
WHERE id = #{id} AND status = 'UNPAID';
-- affected rows = 0 表示已被其他请求处理，视为幂等
```

**唯一索引**最可靠：在表上建 `(biz_id, scene)` 唯一键，重复插入直接抛 `DuplicateKeyException`，捕获后即视为幂等成功而非系统错误——很多人错把它当异常抛出去。**状态机 CAS**适合有明确状态流转（如 `待支付→已支付`）：`UPDATE ... WHERE status = ?`，看**影响行数**判断（0 行说明已被他人处理）。推荐姿势：**Token 防重复提交（挡掉用户层重复）+ 唯一索引兜底（挡掉所有层重复）+ 状态机防乱序（保证状态只前进不回退）**。幂等键设计：`业务主键 + 场景 + 时间窗`（如 `order_123_pay_20260913`），过期清理防重表膨胀。**重要区分**：幂等 ≠ 并发安全，幂等解决"重复执行结果一致"，并发安全还要靠锁/原子操作解决"同时执行"的问题。

**可接受的关键结论**
- 幂等必须在服务端做，前端禁用按钮挡不住网关/MQ 层重试。
- Token 删除用原子 `DEL`，先查后删在并发下会失效。
- 唯一索引最可靠，冲突要捕获 `DuplicateKeyException` 而非当错误抛。
- 推荐组合：Token 防重提交 + 唯一索引兜底 + 状态机防乱序。
- 幂等 ≠ 并发安全，两者需分别处理。

**考察意图**
考察方案边界认知：能否分清三种幂等手段的适用面与代价，而非只会用一种。分水岭是能否点出"幂等 ≠ 并发安全"以及 Token 删除必须原子 `DEL`。

**常见错误回答**
- ❌ "前端禁用按钮就能幂等" —— 错在网关重试与 MQ 重投绕过了前端；正确做法是服务端幂等。
- ❌ "删除 token 用先查后删更安全" —— 错在并发下两个请求都能通过检查；正确做法是原子 `DEL`。
- ❌ "唯一索引冲突就当系统错误抛异常" —— 错在把幂等成功当成故障；正确做法是捕获 `DuplicateKeyException` 视为重复提交成功。
- ❌ "用了幂等就不需要并发控制了" —— 错在幂等不解决同时执行的竞态；正确做法是幂等 + 锁/原子操作各管一段。
- ❌ "状态机 CAS 看返回值布尔就行" —— 错在应看影响行数判断状态是否真的被推进；正确做法是 `WHERE status=?` 后检查 affected rows。

---

## 如何用好这一模块：场景题答题方法论

> 场景题和八股题的评分逻辑完全不同。八股题考"你知不知道"，场景题考"你在不确定里怎么决策"。

### 场景题 vs 八股题：面试官到底在评什么

八股题你背熟就能拿分，场景题背不到——它会不断给你加压、改条件、逼你承认代价。下面这张表先把评分逻辑摆清楚，你才知道力气该往哪使。

| 维度 | 八股题 | 场景题 |
| --- | --- | --- |
| 考察目标 | 知不知道某个知识点 | 在不确定和约束里怎么决策 |
| 答错的后果 | 暴露知识盲区，扣分项明确 | 暴露思维方式和工程成熟度 |
| 是否可以背 | 大量可背 | 背不到，追问即穿帮 |
| 追问的作用 | 验证记忆深度 | 验证边界、权衡、诚实度 |
| 得分天花板 | 答对即满分 | 没有满分，权衡讲得好才高分 |

结论：八股题是"会不会"，场景题是"靠不靠谱"。面试官宁可要一个能把 80 分方案讲清楚、兜底想周全的人，也不要一个背得出最优架构但一被追问就卡壳的人。

### 答题四步法

每次开口，按这个顺序走，能覆盖绝大多数场景题的得分点。

① **复述约束**：开口先把量级、数据规模、一致性要求复述一遍，并主动追问缺失的约束。例如"这个接口允许少卖吗""能接受多久的数据不一致""峰值 QPS 大概多少"。主动追问本身就算加分——它证明你在定义问题，而不是急着给答案。
② **给主方案**：先给一个今天就能落地的 80 分方案，不要一上来堆"最终一致性 + 分库分表 + 多活"的最优架构。能上线的方案比完美的 PPT 值钱。
③ **讲权衡**：明确说出你放弃了什么、代价是什么。是放弃了一致性换延迟，还是放弃了简单性换来扩展性？说不出代价的方案，面试官默认你没想清楚。
④ **补兜底**：失败了怎么办、怎么发现、怎么补偿、怎么回滚。主链路讲完不讲兜底，等于只做了上半场。

### 面试官追问的四种动机

追问不是为难你，是有明确目的。看清动机，才知道往哪个方向接。

| 动机 | 典型问法 | 你应该怎么接 |
| --- | --- | --- |
| 验证深度 | "你说的这个参数具体生效机制是什么？" | 往细节钻，给出参数名、命令、具体指标，别用"大概""应该"糊弄 |
| 验证边界 | "如果流量翻 10 倍 / 发生网络分区呢？" | 承认条件变化，重新评估方案，展示条件敏感度 |
| 验证权衡 | "你这么做放弃了什么？" | 主动说出代价，展示取舍意识，而不是说"没有缺点" |
| 验证诚实度 | "这个你实际线上做过吗？" | 没做过就明说，讲清你会怎么验证，绝不硬编 |

### 常见失分点 TOP 8

1. **上来就说"用 Redis 分布式锁"，但不说锁粒度、超时、失败重试** —— 失分：方案不可落地，像背过名词。怎么改：先说锁的是哪个资源、粒径多大、拿不到锁怎么办、过期了任务还没跑完怎么办。
2. **说"最终一致"却说不出对账和补偿怎么落地** —— 失分：只抛概念没工程细节。怎么改：讲对账任务频率、差异怎么发现、补偿如何保证幂等。
3. **方案里全是"高性能""高可用"这种无数字的形容词** —— 失分：无法判断量级与可行性。怎么改：给 QPS、数据量、RT 目标、可容忍丢失量等具体数字。
4. **一上来堆最优架构，不做 80 分方案** —— 失分：落地性差、不务实。怎么改：先给能今天上线的方案，再谈演进路径。
5. **不主动追问缺失约束** —— 失分：遗漏关键边界条件。怎么改：开口先复述约束并追问"允许少卖吗""能接受多久不一致"。
6. **只讲主链路不补兜底** —— 失分：没考虑失败与回滚。怎么改：讲失败怎么发现、怎么补偿、怎么回滚。
7. **被追问边界就改口或硬编没做过的** —— 失分：暴露不诚实或不严谨。怎么改：承认没做过，讲清楚验证路径。
8. **给的方案无法排查、没有埋点和指标** —— 失分：线上不可观测。怎么改：设计阶段就带监控、`traceId`、告警阈值。

### 怎么用这一模块自测

1. **遮住答案先自己答**：按四步法完整走一遍，别急着看参考答案。
2. **对照"可接受的关键结论"逐条打分**：缺一条扣一分，低于 3 条说明没答到点上。
3. **"渐进式追问"要逐条口头答一遍**，而不是看一遍——真实面试里追问才是分水岭，眼看和嘴答是两回事。
4. **答完反问自己**：我的方案有数字吗？有兜底吗？能排查吗？三项都"有"才算过关。
5. **同一题隔一周再答一次**：看权衡是否讲得越来越清楚，这才是能力增长。

---

## 第 16 章 线上排障与性能调优场景

> 本章所有题目都从"用户感受到的现象"出发。答题时先界定故障发生在哪一段，再谈排查手段——一上来就说"加机器""重启"是最典型的失分回答。

#### 16.1 【接口超时】用户反馈下单偶发超时，上游 3s 超时、下游 RT 显示正常，怎么定位？
> **难度**：L3 深入 ｜ **考察频次**：高 ｜ **目标能力**：故障定位 ｜ **标签**：链路追踪、超时、连接池

**业务场景与约束**
- **业务现象**：用户侧偶发下单超时，上游网关 3s 硬超时触发；下游服务监控面板"RT 一切正常"；客服工单集中在晚高峰。
- **量化约束**：超时率 0.3%；只发生在 20:00–22:00；日均 50 万单；服务端平均 RT 仍显示 55ms；下游监控显示 P99 仅 60ms、错误率 0。

**渐进式追问**
1. 超时率只有 0.3% 且据说是"偶发"，你会先用什么指标确认它发生在客户端、网关、服务端还是下游，而不是先怀疑代码？
2. 平均 RT 55ms 但用户感知超时，这说明耗时分布上有什么特征？你会盯 P99 还是均值？
3. 下游监控"一切正常"为什么可能是监控口径在骗你？你怎样用链路追踪拿到真实的分段耗时？
4. 如果定位到服务端但 CPU 不高，你会优先看线程池队列还是数据库连接池？怎么量化它们的等待？
5. 日志里为什么必须带 `traceId` 且按"网关→服务→下游→DB"分段记耗时，而非只记总耗时？
6. 晚高峰 20–22 点才出现，GC 停顿和连接池争用哪个更可能？你怎么用 `jstat` 和 HikariCP 指标排除？
7. 根治之后，你靠什么监控防止它复发——错误采样率还是全量埋点？

**参考答案要点**
- **定位路径**：先界定故障发生在哪一段 → 用 P99 而非均值确认长尾 → 用分段耗时工具证实 → 量化等待资源 → 根治并加监控。顺序不能乱，先界定再深挖，否则容易在错误组件上花时间。
- **界定范围**：用网关访问日志（带 `traceId`）对比服务端收到请求的时间戳，若差距大就是网络 / DNS / 网关层；若服务端收得到但处理慢，才进服务端排查。
- **看长尾**：平均 55ms 但用户超时，说明是 P99 长尾而非均值问题。监控必须按 `P99` 或错误采样，全量均值会把尖刺抹平——这正是 0.3% 超时查不到的根因。
- **分段耗时**：下游"RT 正常"常是口径骗局——下游只统计已到达它的请求，网关到下游的排队、建连、 DNS 它看不到。链路追踪（`traceId` 贯穿全链路）才能拿到"网关→服务→下游→DB"各段真实耗时。
- **量化等待**：CPU 不高时优先看线程池队列长度（等待线程数）和连接池 pending（`HikariCP` 的 `connectionTimeout`、`activeCount`，Druid 的 `waiting`）。GC 停顿看 `jstat -gcutil` 的 `YGCT`/`FGCT`。
- **根因假设**：晚高峰连接池争用或 `TIME_WAIT` 堆积，用 `netstat -an | grep TIME_WAIT | wc -l` 验证。

```bash
jstat -gcutil <pid> 1s          # 看 GC 次数与停顿累计
netstat -an | grep TIME_WAIT | wc -l   # 验证 TIME_WAIT 堆积
```

**可接受的关键结论**
- 平均 RT 正常但超时，必须按 P99 或错误采样排查，均值会掩盖长尾。
- "下游 RT 正常"常是口径骗局，下游只统计已到达请求，排队与建连它看不到，必须靠 `traceId` 分段耗时。
- 服务端 CPU 不高时优先排查线程池队列与数据库连接池 pending，而非盲猜代码慢。
- 日志必须带 `traceId` 且分段记耗时，否则无法定位慢在哪一跳。
- 复发防护靠错误采样告警 + 连接池 / 队列长度监控，而非全量埋点。

**考察意图**
验证"从现象倒推"的系统化定位能力。L2 与 L3 的分水岭在"下游 RT 正常"那一句：能指出监控口径会骗人、坚持用分段耗时和 P99 证实的是 L3；只说"看日志找慢在哪里"的是 L2。前者展现对监控盲区和方法论的意识，后者停留在操作层面。

**常见错误回答**
- ❌ "先重启试试，不行再加机器" —— 错在把现场证据洗掉且没定位就扩容；正确是先界定故障段再量化。
- ❌ "平均 RT 才 55ms 说明没问题" —— 错在用均值掩盖 P99 长尾；正确盯 P99 / 错误采样。
- ❌ "下游说正常就是下游没问题" —— 错在信监控口径；下游只统计已到达请求，排队建连它看不到。
- ❌ "加个 Redis 缓存就好了" —— 错在没定位就上方案；超时未必是 DB 慢，可能是连接池争用。
- ❌ "日志打总耗时就够了" —— 错在无法分段定位；必须 `traceId` 分段记各跳耗时。

---

#### 16.2 【CPU 飙高】Java 进程 CPU 900%，5 分钟内定位到代码行
> **难度**：L2 扎实 ｜ **考察频次**：高 ｜ **目标能力**：工具链熟练度 ｜ **标签**：CPU、线程栈、jstack、Arthas

**业务场景与约束**
- **业务现象**：Java 进程 CPU 冲到 900%，接口大量超时，业务告警；运维想重启但被叫停。
- **量化约束**：容器 8C；CPU 900%（约等于吃满 7.2 核）；要求 5 分钟内定位到具体代码行；**不能重启、要保留现场**；GC 日志无异常、无 Full GC。

**渐进式追问**
1. 不能重启且要保现场，你第一步会动哪个工具，为什么不能先重启？
2. `top` 只能看到进程级 CPU，怎么下钻到具体线程？
3. `jstack` 看到的线程 `nid` 是 16 进制，怎么把 `top` 里的 tid 对应上？
4. 为什么 `jstack` 抓一次就下结论是不可靠的，要看什么？
5. 除了 `jstack`，Arthas 和 async-profiler 各自适合什么场景？
6. GC 日志无异常，你能排除哪些 CPU 飙高的成因？

**参考答案要点**
- **保现场优先**：容器 8C 但 CPU 900% 且不能重启，第一步是保留证据——先做线程 dump 和（必要时）heap dump，再考虑恢复。重启即销毁现场，复盘只能靠猜。
- **线程级下钻**：`top -Hp <pid>` 找出 CPU 最高的线程 tid，用 `printf '%x\n' <tid>` 转 16 进制得到 `nid`，再 `jstack <pid> | grep -A 20 "nid=0x..."` 定位栈帧。注意 `top` 只到进程，必须 `-H` 到线程。
- **多次采样**：单次 `jstack` 可能抓到处于安全点的线程造成误判，要在 1–2 秒内连抓 2–3 次，取交集确认同一方法持续占 CPU。
- **替代与增强**：`jcmd <pid> Thread.print` 是 JDK 9+ 推荐的等价命令；Arthas `thread -n 3` 直接列出最忙 3 个线程，免手动转 nid；async-profiler 出火焰图，适合找"热点方法"而非单点死循环。
- **常见成因**：死循环、正则灾难性回溯、`Thread.sleep`/锁竞争导致的大量上下文切换、频繁 GC（本例 GC 日志正常可排除）、序列化热点。
- **根因确认**：多次采样都指向同一方法 + 火焰图热点吻合，即可定位；必要时 `jcmd <pid> GC.heap_dump`（注意会 STW）留存。

```bash
top -Hp <pid>                       # 找高 CPU 线程 tid
printf '%x\n' <tid>                 # tid 转 16 进制 nid
jstack <pid> | grep -A 20 "nid=0x..."  # 定位栈帧
```

**可接受的关键结论**
- 先 thread dump / heap dump 保现场，再考虑重启——重启即销毁证据。
- `top -Hp` 下钻到线程，`printf` 转 16 进制后 `jstack` 搜 `nid` 定位栈。
- 单次 `jstack` 不可靠，要多次采样取交集，避免安全点误判。
- GC 日志正常时应优先排查死循环、正则回溯、上下文切换，而非盲调堆。
- Arthas `thread -n 3` 和 async-profiler 火焰图能更快锁定热点方法。

**考察意图**
验证工具链熟练度与"保现场"意识。L2 分水岭在会不会先用 `top -Hp` → `jstack` 这条标准链路，并知道多次采样；L3 还会补一句"先 dump 再重启、用火焰图确认热点"。只说"用 `top` 看一眼"或"重启试试"的，说明没有现场意识。

**常见错误回答**
- ❌ "直接 `jstack` 一次就定位了" —— 一次可能抓到安全点假象；正确多次采样取交集。
- ❌ "CPU 高先重启" —— 重启销毁现场无法复盘；正确先 dump 再恢复。
- ❌ "肯定是 GC，调 `-Xmx`" —— GC 日志已无异常还盲调堆；正确先定位线程栈热点。
- ❌ "用 `top` 看进程就够" —— `top` 只到进程看不到线程；要 `top -Hp` 到线程再转 nid。
- ❌ "加机器扩核" —— 死循环扩核只会更卡；先找到热点方法再说。

---

#### 16.3 【Full GC】每隔 10 分钟 Full GC 一次，每次 STW 800ms
> **难度**：L3 深入 ｜ **考察频次**：中 ｜ **目标能力**：内存分析 ｜ **标签**：Full GC、G1、MAT、堆外

**业务场景与约束**
- **业务现象**：服务每隔约 10 分钟一次 Full GC，接口 P99 出现周期性尖刺；老年代占用呈锯齿状。
- **量化约束**：堆 4G、G1 收集器、JDK 21；每次 Full GC STW 约 800ms；老年代占用升到 80% 后被回收；接口 P99 周期性从 50ms 跳到 800ms+。

**渐进式追问**
1. 你会先用哪条命令确认 Full GC 的频率和停顿，而不是凭感觉调参？
2. 怎么判断这是"内存泄漏"还是"容量不足"？看哪个指标？
3. 要做堆分析，为什么用 `jcmd` 而不是已废弃的旧命令？
4. MAT 里应该看 shallow size 还是 dominator tree，为什么？
5. G1 下有哪些参数和对象类型会加剧 Full GC，比如 Humongous？
6. 你定位到泄漏源后，根治和临时止血分别怎么做？

**参考答案要点**
- **先确认再调参**：用 `-Xlog:gc*:file=gc.log:time,uptime,level,tags` 和 `jstat -gcutil <pid> 1s` 拿到 Full GC 频率与停顿，而不是凭"感觉慢"就加堆。
- **泄漏 vs 容量**：关键看每次回收后老年代基线——锯齿但能回到底部是容量不足（对象该回收的都回收了），只升不回、基线持续抬升是内存泄漏。这是 L2/L3 的分水岭。
- **堆快照**：`jcmd <pid> GC.heap_dump /tmp/heap.hprof`（注意会 STW，选低峰）。不要用已废弃的 jmap 堆统计子命令。MAT 看 **dominator tree（支配树）** 找支配者，而非 shallow size——支配者才是"掐住它就能释放一大片"的真凶。
- **常见泄漏源**：ThreadLocal 未 `remove`、静态集合持续 add、未关闭的连接 / 流、堆外内存、类加载器泄漏（热部署常见）。
- **G1 要点**：`MaxGCPauseMillis` 设目标、`InitiatingHeapOccupancyPercent`（IHOP）控制并发标记触发点；**Humongous 对象**（超过 region 50%）会绕过年轻代直接进入老年代并触发混合回收混乱，是大对象分配的雷区。JDK 25 起 G1 全环境默认分代。
- **根治**：泄漏就修掉未释放的引用；容量不足就扩堆或调高 IHOP，让并发标记提前。

```bash
-Xlog:gc*:file=gc.log:time,uptime,level,tags
jstat -gcutil <pid> 1s
jcmd <pid> GC.heap_dump /tmp/heap.hprof
```

**可接受的关键结论**
- 先用 `-Xlog:gc*` 和 `jstat -gcutil` 确认，严禁用 JDK 8 时代已废弃的 GC 打印参数。
- 区分泄漏与容量看老年代回收后基线：只升不回是泄漏，锯齿回底是容量不足。
- 堆分析用 `jcmd GC.heap_dump` + MAT 支配树，不看 shallow size。
- G1 要关注 Humongous 对象（> region 50%）和 IHOP 设置。
- 容量不足扩堆或调 IHOP，泄漏必须修掉未释放引用，加堆只是延后爆发。

**考察意图**
验证内存问题归因能力。L2 与 L3 的分水岭在能否区分"泄漏"与"容量不足"：只看 GC 频繁就加堆的是 L2，看老年代基线抬升判断泄漏、并用支配树定位真凶的是 L3。后者展现对内存生命周期的理解，前者停留在"加内存"的本能反应。

**常见错误回答**
- ❌ "Full GC 频繁就加堆到 8G" —— 若是泄漏，加堆只是延后爆发；先判断泄漏 vs 容量。
- ❌ "用已废弃的 GC 详情打印参数看日志" —— 该写法 JDK 9+ 已废弃；用 `-Xlog:gc*`。
- ❌ "用已废弃的 jmap 堆统计子命令看堆" —— 该命令过时且有风险；用 `jcmd GC.heap_dump` + MAT。
- ❌ "老年代 80% 就回收是正常的" —— 锯齿回升到 80% 且接口周期尖刺，要查 Humongous 或 IHOP。
- ❌ "MAT 看最大的是谁就行" —— 要看 dominator tree 支配者，shallow size 会误导。

---

#### 16.4 【线程池打满】队列积压、拒绝策略疯狂触发
> **难度**：L1 基础 ｜ **考察频次**：高 ｜ **目标能力**：容量与隔离 ｜ **标签**：线程池、拒绝策略、隔离、熔断

**业务场景与约束**
- **业务现象**：高峰时段线程池队列持续积压，拒绝策略每分钟触发上万次，部分请求直接被拒。
- **量化约束**：核心线程 20、最大 50、队列 2000；峰值流量是平时 3 倍；拒绝次数每分钟上万；下游依赖响应正常。

**渐进式追问**
1. 队列在涨、任务也在完成，你怎么判断是"任务变多"还是"下游变慢"？
2. 为什么无界队列在大流量下是定时炸弹，和本例的有界队列有什么区别？
3. 四种拒绝策略分别适合什么场景，CallerRunsPolicy 为什么可能反噬上游？
4. 一个线程池跑所有业务接口会有什么问题，怎么改？
5. 队列长度和拒绝次数要不要监控，监控不到会怎样？
6. 虚拟线程能不能直接解决这次的问题？

**参考答案要点**
- **先分根因**：看队列增长速率 vs 单任务耗时。若耗时不变、队列只涨，是任务变多（下游没慢）；若耗时涨、队列同步涨，是下游变慢拖住线程。本例下游正常，说明是流量 3 倍导致的任务量暴涨。
- **无界队列是雷**：用 `LinkedBlockingQueue` 不指定容量等于 `Integer.MAX_VALUE`，任务无限堆积最终 OOM。本例用有界队列 2000，所以表现为拒绝而非 OOM——有界反而更"诚实"地暴露问题。
- **拒绝策略取舍**：`AbortPolicy`（默认，抛异常）、`CallerRunsPolicy`（让调用方线程自己跑被拒任务）、`DiscardPolicy`（静默丢）、`DiscardOldestPolicy`（丢最老）。`CallerRunsPolicy` 看似友好，但若调用方是 Tomcat 业务线程，会把它也占满，反而拖垮整个 HTTP 入口——这是典型的反噬。
- **隔离**：不同业务必须用不同线程池，否则一个慢接口占满线程会拖垮全部接口。
- **监控与兜底**：队列长度、活跃线程数、拒绝次数必须上报告警；配合动态调参、降级与熔断。
- **虚拟线程边界**：虚拟线程能解"线程数不够"，但解不了"下游能力不足"——下游慢，换虚拟线程照样阻塞。

**可接受的关键结论**
- 先用队列增长速率 vs 单任务耗时区分"任务变多"还是"下游变慢"。
- 无界队列是 OOM 定时炸弹，必须显式容量；有界队列拒绝是更诚实的暴露。
- `CallerRunsPolicy` 可能用调用方线程跑任务，反噬 Tomcat 入口，需评估后使用。
- 不同业务必须隔离线程池，避免一个慢接口拖垮全部。
- 虚拟线程解线程数瓶颈，解不了下游能力不足；队列与拒绝次数必须监控。

**考察意图**
验证容量与隔离的基本功，这是本模块的 L1 锚点，应届生也应答出。分水岭在知不知道无界队列是 OOM 定时炸弹、`CallerRunsPolicy` 会反噬上游——能讲清这两点的说明真正用过线程池而非背过定义；只说"用线程池提高性能"的，说明停留在概念层。

**常见错误回答**
- ❌ "队列设成无界 `Integer.MAX_VALUE` 不就好了" —— 无界队列是 OOM 定时炸弹；必须显式容量。
- ❌ "拒绝策略用 CallerRuns 最安全" —— 它会用调用方线程跑任务，若调用方是 Tomcat 线程会拖垮 HTTP；要评估。
- ❌ "一个线程池跑所有任务" —— 慢接口会拖垮全部；必须按业务隔离。
- ❌ "拒绝就丢，用户重试" —— 丢弃 + 重试放大可能雪崩；要降级 + 熔断。
- ❌ "虚拟线程能解决一切" —— 虚拟线程解线程数不够，解不了下游能力不足。

---

#### 16.5 【慢 SQL】订单列表查询从 20ms 涨到 3s
> **难度**：L2 扎实 ｜ **考察频次**：高 ｜ **目标能力**：索引与执行计划 ｜ **标签**：慢SQL、执行计划、联合索引、分页

**业务场景与约束**
- **业务现象**：订单列表查询在大用户身上延迟从 20ms 飙到 3s，小用户正常。
- **量化约束**：单表 2 亿行；SQL 为 `WHERE user_id = ? AND status = ? ORDER BY create_time DESC LIMIT 20`；只在部分用户身上复现；慢日志阈值 1s 已开启采样。

**渐进式追问**
1. 你会先跑什么命令看这条 SQL 的执行计划，重点看哪几列？
2. 现有查询要快，联合索引应该怎么建，顺序有什么讲究？
3. `ORDER BY create_time` 会不会导致 filesort，怎么消掉？
4. 深分页 `LIMIT 100000, 20` 有什么代价，游标分页怎么写？
5. 哪些常见写法会让索引失效，比如函数、隐式转换、前导模糊？
6. 为什么只在部分用户复现，是索引问题还是数据问题？

**参考答案要点**
- **看执行计划**：`EXPLAIN` 重点看 `type`（要到 `ref`/`range` 才算用到索引）、`key`（实际走的索引）、`rows`（扫描行数）、`Extra`（警惕 `Using filesort` 和 `Using temporary`）。`Extra` 里的 filesort 往往比 `rows` 更致命。
- **联合索引**：建 `(user_id, status, create_time)` 满足最左前缀，`WHERE` 两列等值 + `ORDER BY create_time` 可被索引消除排序。若把 `status` 放 `create_time` 之后，排序字段断掉，`ORDER BY` 就退化成 filesort。
- **覆盖索引**：`SELECT` 只取索引列（或包含列）可避免回表，进一步降延迟。
- **深分页代价**：`LIMIT 100000, 20` 要先扫 10 万行再丢弃，越翻越慢。改写游标分页 `WHERE create_time < ? ORDER BY create_time DESC LIMIT 20`，或延迟关联先查 ID 再 `JOIN`。
- **失效写法**：函数包裹列（`DATE(col)`）、隐式类型转换（`user_id` 是字符串却传数字）、`OR` 拼接、`LIKE '%x'` 前导模糊、不等号 `!=` 都可能让索引失效。
- **部分用户复现**：数据倾斜（大用户订单几百万行）+ 优化器选错索引，导致小用户走索引、大用户走全表。`ANALYZE TABLE` 更新统计或临时 `FORCE INDEX` 救急，根治是改索引设计。

```bash
EXPLAIN SELECT * FROM orders
  WHERE user_id=? AND status=?
  ORDER BY create_time DESC LIMIT 20;
```

**可接受的关键结论**
- 用 `EXPLAIN` 看 `type`/`key`/`rows`/`Extra`，`Using filesort` 比 `rows` 更值得警惕。
- 联合索引 `(user_id, status, create_time)` 满足最左前缀并消除排序，顺序不能乱。
- 深分页 `LIMIT 100000,20` 代价大，用游标分页或延迟关联改写。
- 函数 / 隐式转换 / 前导模糊 / `OR` 会让索引失效，保持类型与写法一致。
- 部分用户复现多为数据倾斜 + 优化器选错索引，靠 `ANALYZE TABLE` 或 `FORCE INDEX` 救急。

**考察意图**
验证索引与执行计划的实战能力。L2 分水岭在会不会用 `EXPLAIN` 的 `Extra` 定位 filesort、会不会用覆盖索引和游标分页解决深分页——能讲清联合索引顺序与排序消除关系的，说明真在线上调过慢 SQL；只说"加索引"三个字的，说明没碰过 2 亿行表的执行计划。

**常见错误回答**
- ❌ "加个索引就行" —— 没说联合索引顺序，`ORDER BY` 可能仍 filesort；要 `(user_id, status, create_time)`。
- ❌ "用 `LIKE '%关键词%'` 也能走索引" —— 前导模糊不走索引，全表扫描；改后缀或上 ES。
- ❌ "深分页 `LIMIT 100000,20` 没事" —— 要扫 10 万行再丢；改游标分页。
- ❌ "`user_id` 传数字没事" —— 列是字符串会被隐式转换导致索引失效；保持类型一致。
- ❌ "看 `rows` 小就是快" —— 要看 `type` 和 `Extra`，filesort / temporary 比 `rows` 更致命。

---

#### 16.6 【OOM】容器半夜被 OOMKilled，但堆监控只用了 60%
> **难度**：L3 深入 ｜ **考察频次**：中 ｜ **目标能力**：内存全局观 ｜ **标签**：OOM、RSS、堆外内存、NMT

**业务场景与约束**
- **业务现象**：容器半夜被 OOMKilled（退出码 137），但 JVM 堆监控峰值只有 1.5G，看似远未到上限。
- **量化约束**：容器 limit 4G；`-Xmx 2.5G`；JVM 堆监控峰值 1.5G；容器 RSS 持续涨到 4G 后被 kill；发生在凌晨批处理时段。

**渐进式追问**
1. 为什么堆只用 60% 还会被 OOMKilled，JVM 占用等于堆占用吗？
2. 堆外内存通常来自哪些地方，怎么确认是堆外漏？
3. 用什么命令看 JVM 各内存区域的真实占用？
4. `-XX:MaxDirectMemorySize` 不设会有什么后果？
5. 容器里 JVM 怎么正确感知 cgroup 限制，避免按宿主机算资源？
6. 给容器留多少余量算工程常识？

**参考答案要点**
- **认知纠偏**：JVM 实际占用 ≠ 堆。容器 RSS = 堆 + 元空间 + 线程栈 + 直接内存 + CodeCache + GC/JIT 开销，**RSS > Xmx 是常态**。本例堆 1.5G 但 RSS 到 4G，说明泄漏在堆外，堆监控当然"正常"。
- **堆外来源**：Netty `DirectByteBuffer`、gRPC、压缩库（如 snappy）、glibc malloc arena 碎片、未释放的 `ByteBuffer`。凌晨批处理时段大量序列化最易触发。
- **量化与诊断**：`-Xmx 2.5G` 给 4G 容器理论留 1.5G 余量，但堆外无上限就会突破。开 `-XX:NativeMemoryTracking=summary` 后用 `jcmd <pid> VM.native_memory summary` 看各区域真实占用。
- **必须显式上限**：`-XX:MaxDirectMemorySize` 不设时默认等于 Xmx，但堆外（如 glibc arena、CodeCache）仍可能涨，必须显式设上限并监控。
- **容器感知**：`-XX:+UseContainerSupport`（JDK 10+ 默认开）让 JVM 读 cgroup；用 `-XX:MaxRAMPercentage=75` 自动按容器 limit 算堆，别写死 `-Xmx` 顶满。
- **工程常识**：给容器留 25–30% 余量，Xmx 别顶满 limit，否则堆外一涨就必死。

```bash
jcmd <pid> VM.native_memory summary   # 需 -XX:NativeMemoryTracking=summary
```

**可接受的关键结论**
- JVM 占用 ≠ 堆，RSS = 堆 + 元空间 + 栈 + 直接内存 + CodeCache + GC/JIT，RSS > Xmx 是常态。
- OOMKilled 但堆低，优先怀疑堆外泄漏（Netty、gRPC、压缩库、arena 碎片）。
- 用 `jcmd VM.native_memory` 看各区域，需开 NMT。
- 必须显式设 `-XX:MaxDirectMemorySize`，否则堆外仍可能涨爆。
- 容器里留 25–30% 余量，用 `MaxRAMPercentage` 而非写死 `-Xmx` 顶满。

**考察意图**
验证内存全局观。L2 与 L3 的分水岭在是否知道 RSS > Xmx 是常态、会不会用 NMT 查堆外、是否显式设 `MaxDirectMemorySize`——知道"堆只占 JVM 一部分"的是 L3，盯着堆监控说"没满不可能 OOM"的是 L2。后者缺的是对进程内存构成的整体认知。

**常见错误回答**
- ❌ "堆才用 60% 不可能 OOM" —— OOM 是容器 RSS 超 limit 被杀，堆外不计入 Xmx；要看 NMT。
- ❌ "不设 `MaxDirectMemorySize`" —— 默认等于 Xmx，但堆外（arena/CodeCache）仍可能涨爆；必须显式设上限。
- ❌ "JVM 占用 = 堆占用" —— RSS 含元空间 / 栈 / 直接内存 / CodeCache；要全局看。
- ❌ "把 `-Xmx` 设到 limit 的 95%" —— 不留余量，堆外一涨就 kill；留 25–30%。
- ❌ "容器里不用 `-XX:+UseContainerSupport`" —— 不感知 cgroup 会按宿主机算核数 / 内存；JDK 10+ 默认开但应确认。

---

#### 16.7 【数据库死锁】两个账户互相转账偶发死锁
> **难度**：L2 扎实 ｜ **考察频次**：中 ｜ **目标能力**：锁与并发 ｜ **标签**：死锁、InnoDB、加锁顺序、重试

**业务场景与约束**
- **业务现象**：转账业务偶发报错 `Deadlock found when trying to get lock`，需重试才能成功。
- **量化约束**：转账 500 TPS；账户表 8000 万行；MySQL 8.0 / InnoDB / RR 隔离 + row 格式 binlog；每天几十次死锁。

**渐进式追问**
1. 死锁的根本成因是什么，和"锁等待超时"是一回事吗？
2. 你用什么命令看到最近一次死锁的等待图和涉及事务？
3. 根治手段是什么，为什么要固定加锁顺序？
4. 缩小事务范围为什么能降低死锁概率？
5. 降低隔离级别到 RC 有什么利弊，binlog 要配什么格式？
6. 业务层加重试就够了吗，重试要注意什么？

**参考答案要点**
- **根因**：死锁是**加锁顺序不一致**导致的环。A 向 B 转（先锁 A 再锁 B）和 B 向 A 转（先锁 B 再锁 A）并发时，各自持有一把等另一把，形成死锁。它和"锁等待超时"不同——死锁是数据库检测到环后自动回滚一方，锁等待是单纯慢。
- **取证**：`SHOW ENGINE INNODB STATUS` 看 `LATEST DETECTED DEADLOCK` 段，能拿到两个事务各自持锁 / 等锁的 SQL 和等待图，据此还原加锁顺序。
- **根治——固定加锁顺序**：转账前按 `account_id` 排序，无论方向都从小到大依次加锁，所有人都同一顺序就无解环。这是比"加重试"更根本的手段。
- **缩小事务**：把查询、非关键操作移出事务，缩短持锁时间，降低两事务重叠窗口。
- **隔离级别**：降到 RC（配合 row 格式 binlog，保证主从一致）可减少间隙锁，降低死锁概率，但业务要能接受不可重复读。
- **重试必须幂等**：死锁是 InnoDB 自动回滚的，业务必须能安全重试——加重试但不解决加锁顺序，还会再死；且重试要带幂等键，避免重复转账。热点账户另走汇总 / 子账户避免单行争用。

**可接受的关键结论**
- 死锁根因是加锁顺序不一致形成环，与锁等待超时不是一回事。
- `SHOW ENGINE INNODB STATUS` 的 `LATEST DETECTED DEADLOCK` 段能看到等待图。
- 根治是按 `account_id` 排序固定加锁顺序，而非只加重试。
- 缩小事务范围、降隔离级别到 RC（配 row binlog）都能降低概率。
- 重试必须幂等；只 try-catch 不解决顺序问题还会再死。

**考察意图**
验证锁与并发正确性意识。L2 分水岭在能否从"加锁顺序"而非"加重试"根治，并理解死锁是数据库自动回滚、业务需幂等重试——能画出等待环、讲清顺序固定化的是 L2+；只说"catch 住重试就行"的，说明没理解死锁是结构性问题而非偶发异常。

**常见错误回答**
- ❌ "死锁就加 try-catch 重试" —— 顺序不解决还会再死；先固定加锁顺序。
- ❌ "用 `SELECT ... FOR UPDATE` 锁全表" —— 锁范围过大并发崩；只锁涉及账户且按 id 排序。
- ❌ "把隔离级别调到串行化" —— 性能腰斩且仍可能死锁；降到 RC 配 row binlog 更实际。
- ❌ "重试不加幂等" —— 重试可能重复转账；必须幂等键。
- ❌ "死锁偶发忽略就行" —— 每天几十次且影响转账，必须根治加锁顺序。

---

#### 16.8 【连环雪崩】一次由慢 SQL 引发的全站不可用，怎么复盘与加固？
> **难度**：L3 深入 ｜ **考察频次**：高 ｜ **目标能力**：系统性思考 ｜ **标签**：雪崩、复盘、熔断、故障演练

**业务场景与约束**
- **业务现象**：一次运营查询拖垮全站，故障持续 40 分钟、影响 12 万用户，事后需要复盘与加固。
- **量化约束**：故障持续 40 分钟；影响 12 万用户；起因是一条未加索引的运营查询；演化路径为连接池耗尽 → 业务线程全部阻塞 → 上游超时重试放大流量 → 全站不可用。

**渐进式追问**
1. 故障发生后你第一件事做什么，为什么不能先重启？
2. 根因链应该怎么画，只写"数据库慢"够吗？
3. 恢复手段的优先级是什么，扩容在第几位？
4. 改进项怎么写才不是空话，要不要有 owner 和验收标准？
5. 复盘文档应该包含哪几块，时间线怎么还原？
6. 这次是慢 SQL，下次可能是别的，怎么让系统不再被单点拖垮？

**参考答案要点**
- **先保现场再恢复**：第一动作是保留证据——thread dump + heap dump + 监控快照 + GC 日志，再考虑恢复。一上来重启把现场洗掉，复盘就只能靠猜，根因永远查不清。
- **画根因链而非写表象**：慢 SQL（未加索引的运营查询）→ 单条查询长时间占用连接 → 连接池耗尽 → 业务线程阻塞等连接 → 上游超时后重试放大流量 → 雪崩。要呈现这条因果链，而不是只写"数据库慢"。这是系统性思维的核心。
- **恢复优先级**：先限流 / 降级止血 → 回滚变更（本次是 kill 掉那条运营查询）→ 必要时扩容。注意：上游重试放大时扩容反而喂更多流量，顺序错了越扩越糟。
- **改进项要可执行有 owner**：连接池监控与告警、SQL 上线前执行计划审核、超时与重试预算（上游重试带退避和上限）、熔断、定期压测与故障演练。每条改进必须有负责人和验收标准。
- **复盘文档结构**：时间线 → 影响面 → 根因 → 改进项 → 验收标准。时间线靠监控快照和日志还原，不是凭记忆。
- **防单点**：单点慢查询能拖垮全站，说明缺隔离与熔断。加固方向是资源隔离（连接池按业务分）、熔断、超时预算，让一个慢点无法耗尽全局资源。

**可接受的关键结论**
- 先 dump 保现场（thread/heap/监控/GC 日志）再恢复，重启会洗掉证据。
- 根因要画因果链：慢 SQL → 连接池耗尽 → 线程阻塞 → 重试放大 → 雪崩，而非只写"数据库慢"。
- 恢复优先级：限流降级止血 → 回滚变更 → 最后才扩容（重试放大时扩容反噬）。
- 改进项必须可执行、有 owner、有验收标准，"以后注意"不是改进项。
- 系统级加固靠资源隔离 + 熔断 + 超时预算 + 压测演练，避免单点拖垮全局。

**考察意图**
验证系统性复盘与加固能力。L3 分水岭在能否画出完整根因链、改进项是否可执行且有 owner——能区分"止血动作"和"根治动作"、把故障归因到架构缺失的是 L3；只说"以后注意 SQL 性能""加机器"的是 L2。前者展现对系统韧性的整体思考，后者停留在单点救火。

**常见错误回答**
- ❌ "一上来先重启恢复" —— 重启洗掉所有现场，复盘靠猜；先 dump 再恢复。
- ❌ "根因就是数据库慢" —— 只写表象不画根因链；要慢 SQL→连接池→线程→重试放大。
- ❌ "改进项写'以后注意 SQL 性能'" —— 无可验收动作和 owner；要具体 + 负责人 + 验收。
- ❌ "加机器防雪崩" —— 重试放大时扩容反而更糟；先限流熔断止血。
- ❌ "复盘文档只写结论" —— 缺时间线 / 影响面 / 验收；按固定结构写全。

---
